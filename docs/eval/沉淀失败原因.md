# Fallback Mechanism & Exception Logs

```yaml
task_id: "TASK-2026-VU-P0"
node: F2.4  # self-check upgrade + fission matrix
updated: 2026-08-03

exception_event:
  node_failed: batch_fission.variant_pairwise_separation
  error_class: DESIGN_FLAW          # not TOOL_ERROR, not threshold mis-tuning
  status: RESOLVED                  # fix landed + measured 2026-08-03T22:00Z
  symptom: "variant-vs-source all pass; variant-vs-variant all fail"
  repro:
    src: assets/test/剑鬼.mp4        # 8.0s 720x1280, heavy, count=3
    vs_source:  [16.75, 17.50, 17.25]   # weak=0.0 -> pass
    pairwise:   {v1_v2: 9.875, v1_v3: 9.25, v2_v3: 3.5}
    worst_pair: {i: 1, j: 2, avg: 3.5, min: 0, weak: 0.875}
  raw_note: "all_pass=False; 3/3 pairs in too_close_pairs"

root_cause:
  statement: "variants are iid low-amplitude perturbations of one source -> far from source (~16-20), near each other (~3-9)"
  mechanism:
    - "trim head drawn iid from ONE narrow band -> inter-variant time offset delta tiny"
    - "剑鬼 8.0s: MAX_TRIM_RATIO caps heavy total to 0.8s -> head delta <= 0.16s"
    - "微笑 5.06s < MIN_DURATION_TRIM -> trim skipped -> delta == 0, time-leg absent (v1_v2 avg=1.0)"
  measured_leverage:                # 16 frames, per-dimension differential
    speed_1.10_vs_0.90: {avg: 1.875, weak: 1.00, verdict: NEAR_IDENTITY}
    rotate_-1_vs_+1:    {avg: 4.12,  weak: 0.81, verdict: TOO_WEAK}
    crop_0.02_vs_0.08:  {avg: 7.50,  weak: 0.44, verdict: TOO_WEAK}
    time_offset_1.0s:   {avg: 29.12, weak: 0.06, verdict: EFFECTIVE}
    flip_h_vs_v:        {avg: 33.00, weak: 0.00, verdict: EFFECTIVE}
  key_insight: "ratio-normalized sampling maps both videos to the same relative instant -> speed changes duration only, and is cancelled by the metric itself. Only time-offset and flip are real separation legs."

resolution:                         # what actually shipped
  action_taken: "SPREAD_TRIM_HEAD_DETERMINISTICALLY_BY_VARIANT_INDEX"
  impl: "_calc_trim(..., phase=None); dedup_video(..., trim_phase=); batch_fission phase=i/(count-1)"
  measured_after:
    asset_8.0s: {heads: [0.0, 0.4, 0.8], pairwise: {v1_v2: 17.375, v1_v3: 23.875, v2_v3: 18.50}, all_pass: true}
    worst_pair_delta: "3.50 -> 18.50"
  tests: "48 passed (41 pre-existing untouched + 7 new phase-semantics)"
  frozen_contract_respected: true   # metrics.py caliber unchanged, gate not lowered
  side_effect_check: "out_dur identical across variants (budget fixed) -> duration_close unaffected"

rollback_checkpoint:
  target_node: F3.1                 # advanced; F2.4 closed
  clear_dirty_data: false           # F2.1-F2.4 verified, keep
  frozen_contract: "metrics.py caliber (avg>=12 & weak<=0.10) NOT to be lowered"
  rejected_alternatives:
    - opt: "lower pairwise threshold"
      why_no: "would also loosen variant-vs-source verdict; PRD 8.2 needs real separation not a rubber stamp"
    - opt: "reverse speed per variant"
      why_no: "disproven - speed is near-identity under this metric (avg 1.875)"
    - opt: "spread crop across bands"
      why_no: "disproven - forced 0.02/0.05/0.08 gave 4.875/5.125/7.75, worse"

# ---- EXT: Yuanbao image upload troubleshooting (2026-08-11) ----
task_id: "TASK-2026-VU-EXT-YUANBAO-UPLOAD"
node: F-配音-007_vision_yuanbao
status: RESOLVED
exception_event:
  node_failed: yuanbao_upload_images
  error_class: INTEGRATION_MISMATCH
  symptom: "multiple approaches (set_input_files, file_chooser with varied selectors, MutationObserver) ALL failed; rewrite worked but images never appeared in Yuanbao prompt"
  discovery_method: "DOM dump analysis + Playwright screenshots at each step"
  
root_cause_chain:
  - layer_1: "Yuanbao is Next.js SPA; NO native <input type=file> in DOM (DOM dump confirmed)"
  - layer_2: "Clicking '+' button opens a 3-option DROPDOWN MENU (图片/本地文件/腾讯文档), NOT a file picker"
  - layer_3: "Clicking '图片' THEN triggers React to create file input / native dialog"
  - layer_4: "JS el.click() fails: React requires real MouseEvent with isTrusted=true"
  
failed_approaches_and_why:
  - approach: "page.locator('input[type=\"file\"]').set_input_files()"
    result: "found 0 file inputs — none existed before menu click"
    hours_wasted: ~3
  - approach: "page.evaluate + dispatchEvent(MouseEvent)"
    result: "React ignored isTrusted=false events"
    hours_wasted: ~1
  - approach: "MutationObserver to catch file input after clicking '+'"
    result: "file input only created after '图片' sub-menu click, not '+' click"
    hours_wasted: ~0.5
  - approach: "page.wait_for_event('filechooser') with 20s timeout"
    result: "timed out — '图片' sub-click was via JS el.click(), never triggered native dialog"
    hours_wasted: ~0.5
  - approach: "page.locator('text=\"图片\"').first.click()"
    result: "menu still visible in after-click screenshot — click selector didn't match DOM"
    hours_wasted: ~0.5

working_solution:
  action: "Playwright real-mouse click on exact-text '图片' menu item + file_chooser interception"
  selector: "page.get_by_text('图片', exact=True).first"
  click: ".click(force=True)"
  intercept: "page.expect_file_chooser(timeout=10s) → chooser.set_files(frames[:3])"
  fallback: "set_input_files on dynamically created <input type=file>"
  key_insight: "Playwright's get_by_text with exact=True maps to React's textContent match; force=True bypasses visibility checks"

lessons_learned:
  - "ALWAYS do DOM dump + screenshot BEFORE coding upload logic for unfamiliar SPAs"
  - "Don't assume <input type=file> exists — modern React apps use custom components"
  - "Don't assume one-click upload — check for multi-step menus/submenus"
  - "JS el.click() is NOT equivalent to Playwright .click() — React checks isTrusted"
  - "MutationObserver is a great diagnostic tool, not a production upload mechanism"
  residual_open_item:               # narrowed, not closed
    scope: "sources < MIN_DURATION_TRIM (7s) only"
    state: "time-leg structurally absent; measured time-only pairwise 0.5-1.75 (fail), flip-spread 28.75-35.88 (pass)"
    current_behaviour: "batch_fission emits separation.hint naming flip_mode as the only lever; does NOT auto-enable flip"
    why_not_auto: "flip is default-off by design (DD 2.3 high-destruction dimension) - auto-enabling needs sign-off"

historical_lessons:
  - issue: "recommended p25 caliber for phash gate before testing its stability"
    disproof: "200x resampling: p25 span 10.0 at n=8, same order-statistic drift as min"
    solution: "any statistical-caliber change must be preceded by a sampling-stability experiment; weak_frame_ratio chosen (span 0.103-0.106 across n)"
  - issue: "concluded 'flip is necessary for passing' from single asset 龙.mp4"
    disproof: "3-asset calibration: medium/heavy pass without flip"
    solution: "never extrapolate a gate decision from one asset"
  - issue: "hypothesized crop constant caused co-directional variant offset"
    disproof: "forcing crop across full band worsened pairwise (4.875/5.125/7.75)"
    solution: "state hypothesis as hypothesis; run the control before writing it as cause"
  - issue: "archived handoff recorded the failing matrix as 微笑.mp4 x3 41s"
    disproof: "recompute: numbers belong to 剑鬼.mp4 8.0s x3; 微笑 has only 2 legacy variants (avg 1.0)"
    solution: "recompute any number before it enters a durable doc; the 8.0s duration is what makes the 10% trim cap the binding constraint"
  - issue: "diagnosed _calc_trim as 'raising on 5s floor'"
    disproof: "it returns early at duration<7s branch; the raise expectation came from a wrong temp smoke script"
    solution: "verify the harness before trusting its failure signal"
  - issue: "-t placed after -i (output-side) per DD 2.5c literal text"
    disproof: "output-side -t is measured AFTER setpts -> tail trim silently swallowed, trim_tail became a false report (12.934s vs 12.940s expected)"
    solution: "-ss/-t both moved before -i (source timeline); trim and speed now orthogonal"
  - issue: "carried '1.0s time offset needed to pass' forward as a global constant"
    disproof: "that figure came from the 49.5s asset; on 剑鬼 8.0s a 0.400s adjacent delta already gives avg 17.4-23.9 (pass)"
    solution: "required delta is content-motion dependent; measure per asset class, never reuse one asset's curve as a threshold constant"
```

---

# 上线/部署问题

```yaml
task_id: "TASK-2026-VU-DEPLOY"
node: PROD  # 生产上线与运维
updated: 2026-08-09

exception_events:

  - event_id: PROD-001
    node_failed: 产物下载
    error_class: ENCODING        # Python http.server 编码限制
    status: RESOLVED
    symptom: "点击下载产物按钮，前端显示'失败'，后端无 HTTP 响应（连接中断）"
    root_cause:
      statement: "py http.server.send_header() 仅支持 latin-1 编码，产物文件名含中文字符（如 1483589377_去重.mp4）导致 UnicodeEncodeError，Handler 直接崩溃"
      crash_trace: "File .../http/server.py, line 522, in send_header: ('%s: %s\r\n' % (keyword, value)).encode('latin-1', 'strict') -> UnicodeEncodeError"
      repro: "curl 'http://localhost:8765/local/download/1483589377_%E5%8E%BB%E9%87%8D.mp4' -> HTTP 000 (连接中断)"
      contrast: "英文文件名 test123.mp4 -> HTTP 200 正常"
    resolution:
      action_taken: "Content-Disposition 改用 RFC 5987 编码"
      before: 'f\'attachment; filename="{safe.name}"\''
      after: "f\"attachment; filename*=UTF-8''{quote(safe.name)}\""
      commit: 23c34c9
    lesson: "Python http.server 的 HTTP 头管线是 latin-1，任何非 ASCII 值必须 URL-encode 后放入 header。不限于文件名——所有自定义头值都要验证编码安全。"

  - event_id: PROD-002
    node_failed: HTTPS 部署后前端无法连接 MCP
    error_class: CONFIG         # 前端硬编码 http:// 与 HTTPS 不兼容
    status: RESOLVED
    symptom: "添加 Let's Encrypt 证书 + nginx 443 反代后，浏览器访问 https://vu.evenblue.top 显示'无法连接 MCP Server'"
    root_cause:
      statement: "app.js 中 API_BASE 硬编码 `http://` 协议前缀。HTTPS 页面加载后 JS 计算 API_BASE = http://vu.evenblue.top，浏览器混合内容安全策略拦截 fetch() 请求（https 页面禁止加载 http 资源）"
      code_defect: "return `http://${h}`  // 不管实际页面协议，永远返回 http"
      impact: "fetch 请求被浏览器拦截，根本未到达 nginx，前端误以为后端未启动"
    resolution:
      action_taken: "API_BASE 改用 window.location.protocol 动态取值"
      before: 'return `http://${h}`'
      after: 'const proto = window.location.protocol; return `${proto}//${h}`'
      commit: edb8451
    lesson: "前端 API_BASE 绝不能硬编码协议。部署 HTTP 时正常工作 ≠ HTTPS 也能工作。混合内容拦截是浏览器级别静默失败，不会在 nginx access log 留痕，只能通过浏览器 DevTools Console 发现。"

  - event_id: PROD-003
    node_failed: 服务器仓库与线上不一致
    error_class: PROCESS        # 部署流程缺失版本追踪
    status: RESOLVED
    symptom: "GitHub main 分支缺少线上已运行的功能（上传/下载/临时目录），仓库 ≠ 服务器"
    root_cause:
      statement: "服务器通过 SFTP 上传文件部署，非 git checkout；部署后未将改动同步回 main 分支，导致仓库滞后于线上"
      gap: "main (c303ef5) 未含 dev 已提交的 4 个 feature commit (36a55f7/cb0f8dd/275584d/2dc55e0)，而服务器运行的是 SFTP 快照等价于 dev 的功能态"
    resolution:
      action_taken: "dev fast-forward merge 进 main，推至 origin/main"
      after: "main = dev = 服务器功能态"
    lesson: "SFTP 部署的项目必须显式维护'哪个分支 = 线上版本'的对应关系。部署后立即把对应分支推至仓库，避免仓库与线上脱节。"

  - event_id: F-配音-006-01
    node_failed: copy_rewriter.playwright_launch
    error_class: DESIGN_FLAW     # 不是网络问题，不是 selector 问题
    status: RESOLVED
    updated: 2026-08-10
    symptom: "浏览器打开后停在 about:blank，DeepSeek 改写永不返回；前端也看不到失败原因"
    repro:
      env: "实测机 27 个 msedge.exe + 12 个 chrome.exe 在运行"
      case_A_dedicated_profile: "干净临时 profile -> goto 成功，落到 chat.deepseek.com/sign_in"
      case_B_user_profile: "用户真实 Edge User Data -> launch_persistent_context 抛 TargetClosedError"
    root_cause:
      statement: "复用用户日常浏览器的 User Data 目录，撞 Chromium 单例锁"
      mechanism: >
        Chromium 对 user_data_dir 加单例锁。已有浏览器占用时，新进程把 URL 交给
        已运行实例后自行退出，不会干净报错 -> Playwright 手里的 page 永远无法导航，
        表现就是 about:blank。原代码把 goto 异常吞掉，_last_error 也就永远拿不到原因。
      design_level_problem: >
        方案本身自相矛盾：它依赖「用户已登录的 Edge profile」，而这恰恰是 Playwright
        在 Edge 运行时唯一打不开的 profile。且未开远程调试端口（9222/9223/9229 均无监听），
        attach 到活动浏览器也不可行。要求用户每次改写前关浏览器不是可用工作流。
    resolution:
      action_taken: "SWITCH_TO_DEDICATED_PROFILE_WITH_ONE_TIME_LOGIN"
      impl: "PROFILE_DIR=station/logs/.deepseek-profile（VU_DEEPSEEK_PROFILE 可覆盖，对齐 pipeline 的 VU_* 约定）+ --login 交互登录 + --probe 自证"
      measured_after: "28 个 Edge 进程在跑，仍成功 about:blank -> chat.deepseek.com/sign_in"
      side_benefit: "改写与日常浏览可并行，用户无需关浏览器"
      gitignore: "profile 落在 station/logs/* 之下，已被 .gitignore:9 覆盖，凭证不会入库"

  - event_id: F-配音-006-02
    node_failed: copy_rewriter.is_logged_in
    error_class: WRONG_ORACLE    # 判据本身选错，越修越复杂
    status: RESOLVED_BY_REMOVAL
    updated: 2026-08-10
    symptom: "--check 报『已登录 True』，但从未登录过"
    attempts_and_falsification:
      - try: "判 Cookies 文件是否存在"
        falsified: "浏览器一启动就建库，未登录也存在"
      - try: "数 deepseek.com 域 cookie 数量 > 0"
        falsified: "匿名访问 /sign_in 即下发 3 个：HWWAFSESID / HWWAFSESTIME / ds_session_id"
      - try: "排除已知匿名 cookie 名单，剩下的算凭证"
        falsified: "复访又多出 smidV2 与 .thumbcache_<hash>，名单随访问次数增长 = 长期猜谜"
    resolution:
      action_taken: "RETIRE_COOKIE_HEURISTIC"
      final: "只保留 has_profile()（是否初始化过，用于省一次浏览器启动）；登录态唯一可靠信号 = 打开页面是否被重定向到 /sign_in"
    lesson: "判外部登录态不要猜存储层特征（cookie 名/文件存在性），要看应用层可观测行为（是否被重定向到登录页）。名单法在对方随时新增字段的场景下必然长期失效。"

  - event_id: F-配音-006-03
    node_failed: copy_rewriter._wait_reply
    error_class: LOGIC_ORDER     # 自测发现，未流到用户
    status: RESOLVED
    updated: 2026-08-10
    symptom: "超时分支先做「保底返回已抓文本」再判 generating -> 仍在流式输出的半截句子会被当成成品"
    impact_if_shipped: "截断文案直接进 TTS 配音，产出半句旁白"
    resolution:
      action_taken: "REORDER_GUARD"
      final: "仅在『已停止生成』时才保底返回；仍在生成 = 抛错，由 pipeline 降级用原文"
    lesson: "降级路径的顺序本身是正确性的一部分：保底逻辑必须先确认数据完整，再决定要不要用。宁可失败降级，不可交付半成品。"

  - event_id: F-配音-006-04
    node_failed: copy_rewriter.headless_ua
    error_class: DETECTION_SURFACE
    status: RESOLVED
    updated: 2026-08-10
    symptom: "headless 模式 navigator.userAgent 自报 HeadlessChrome/151.0.0.0"
    why_it_matters: "DeepSeek 前置华为 WAF（HWWAFSESID 即其会话 cookie），HeadlessChrome 是标准爬虫特征；且此坑只会在用户登录之后才暴露"
    resolution:
      action_taken: "STRIP_HEADLESS_MARKER_FROM_REAL_UA"
      impl: "启动前先探真实 UA，replace('HeadlessChrome/','Chrome/') 回传；版本号不硬编码，随浏览器升级自动跟进"
      verified: "抓 chat.deepseek.com 实际请求头确认为普通 Edge"
    lesson: "把 headless 设为默认时，必须同时检查它引入的可探测特征。UA 版本号硬编码会随浏览器升级变成新的假身份。"

  - event_id: F-配音-006-05
    node_failed: copy_rewriter -> tts_client 文本交接
    error_class: INTERFACE_ASSUMPTION
    status: RESOLVED
    updated: 2026-08-10
    symptom: "DeepSeek 用 <p> 分块，抓回文本含 \\n；tts_client.tts() 不做任何清洗 -> 朗读出停顿"
    also_covered: ["markdown 星号会被读出来", "「改写后的文案：」前缀", "整段被引号包裹"]
    resolution:
      action_taken: "ADD_CLEAN_REPLY_AT_PRODUCER_SIDE"
      impl: "_clean_reply()：合并行 + 去 markdown + 去前缀 + 剥包裹引号；中文间不留空格、英文词间空格保留（8 用例覆盖）"
    lesson: "跨模块交接文本前先确认下游是否清洗。生产侧清干净比让消费侧兜底更可靠——tts_client 是纯传输，不该承担文案规整。"

  - event_id: F-配音-006-06
    node_failed: copy_rewriter.__main__ 控制台输出
    error_class: PLATFORM_ENCODING
    status: RESOLVED
    updated: 2026-08-10
    symptom: "--probe 打印 ❌ 抛 UnicodeEncodeError: 'gbk' codec can't encode character '\\u274c'"
    resolution:
      action_taken: "WRAP_STDOUT_UTF8"
      impl: "按 hooks/pre_tool_guard.py 同法 io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')"
    lesson: "SAD §10.5 已记录 Windows GBK 陷阱，但只在 hooks 三处落实；任何新增 CLI 入口都要同样包一层，emoji/中文都会触发。"

historical_lessons_deploy:
  - issue: "以为'everything up-to-date'表示已推送——实际 dev 分支缺少 local remote-tracking ref 导致 git branch -a 看不到 origin/dev"
    solution: "验证远程分支用 git ls-remote origin <branch>，不依赖本地 remote-tracking refs"
  - issue: "部署后测试下载接口用了英文文件名通过，未覆盖中文文件名场景"
    solution: "产物文件名可能含中文（如 _去重.mp4），冒烟测试必须覆盖非 ASCII 文件名"
  - issue: "HTTPS 证书部署后只测了静态页面加载，未测前端 JS 中的 fetch() 路径"
    solution: "HTTPS 上线冒烟清单：静态页面 + MCP discover + 上传 + 下载 + 浏览器 DevTools Console 无混合内容警告"
```

```yaml
task_id: "VU-EXT-配音链"
node: F-配音-006_deepseek_rewrite_text_injection
updated: 2026-08-11

exception_event:
  node_failed: _fill_and_send.text_fill
  error_class: REACT_EVENT_TRUST_BYPASS
  raw_exception: "输入框空白 — JS dispatchEvent(InputEvent) isTrusted=false -> React onChange 未触发"

rollback_checkpoint:
  target_node: _fill_and_send
  clear_dirty_data: false
  action: "REPLACE_JS_DISPATCHEVENT_WITH_PLAYWRIGHT_NATIVE_FILL"

root_cause_analysis:
  symptom: "跳转 DeepSeek 后 textarea 空的，JS dispatchEvent(KeyboardEvent Enter) 也无效"
  cause_chain:
    - "React textarea 绑定 onChange 仅响应 native InputEvent（isTrusted: true）"
    - "page.evaluate + dispatchEvent(new InputEvent(...)) 生成合成事件，isTrusted 必定为 false"
    - "同理 dispatchEvent(new KeyboardEvent('keydown',{key:'Enter'})) isTrusted=false -> React 不响应"
    - "仅 page.locator(...).fill() / page.keyboard.press() 通过 WebDriver Actions API 发出 os-level 真实事件"

fix:
  fill: "page.locator(SEL_EDITOR).first.fill(prompt)   # Playwright Actions API, isTrusted=true"
  send: "page.keyboard.press('Enter')                 # os-level keystroke, isTrusted=true"
  fallback: "findSendButton 补 compositional query（composer 内任意可见按钮）"

historical_lessons:
  - issue: "React onChange/value 绑定的元素不能用 JS value= + dispatchEvent 写入"
    solution: "一律用 Playwright locator.fill()；所有 DOM 交互优先 Playwright Actions API，最后 resort 才 evaluate"
  - issue: "React onKeyDown 绑定的元素不能用 JS dispatchEvent(KeyboardEvent) 触发"
    solution: "直接用 page.keyboard.press()；isTrusted 是 Web 平台底层限制，无法绕过"
  - issue: "前端 rewrite 预览失败只靠 toast，用户根本看不到原因"
    solution: "失败信息直接注入预览区 DOM 持久显示，并附带诊断数据截屏（URL/title/textarea 状态）"

proof: "2026-08-11 用户 live 测试：login -> preview -> browser open -> locator.fill 写入文字 -> keyboard.press Enter -> DeepSeek 回复 -> 前端展示改写文案 PASS"
```

```yaml
task_id: "VU-EXT-配音链"
node: F-配音-008_server_headless_qr_login
updated: 2026-08-11
status: CLOSED_UNRESOLVED           # 服务器 headless Chromium 元宝 QR 登录 = 死胡同

exception_event:
  node_failed: yuanbao_server_login_cookie_persistence
  error_class: PLATFORM_LIMITATION  # 非代码 bug，平台能力边界
  symptom: "扫码成功、cookies 文件存在 (20480 字节)、但新 Chromium session 打开元宝显示'未登录'"
  repro_steps: "服务器 Linux → launch_persistent_context → 截 QR iframe → 用户手机扫码 → 确认授权 → browser 跳到元宝 chat 页 → 查看 profile cookies 文件 → 只有匿名指纹无 auth token"

root_cause_analysis:
  - layer_1_cookie_exists: "Cookies 文件确实落盘 (20480 bytes)，且 --disable-encryption-cookies 确认未加密"
  - layer_2_cookie_content_empty: "文件内仅含匿名指纹 cookie: _qimei, _ga, _TDID_CK, _gcl_au 等。无元宝登录 session token。"
  - layer_3_why_no_auth_token: >
      元宝 QR 登录流程: 元宝页面 → 弹出 open.weixin.qq.com/connect/qrconnect iframe
      → 微信扫码授权 → 302 redirect 回 yuanbao.tencent.com → Set-Cookie
      但 headless Chromium 在处理跨域 iframe 内的 Set-Cookie / redirect 链时行为不可靠。
      也可能元宝用 localStorage / sessionStorage / Service Worker 存 auth，而非纯 cookie。
  - layer_4_not_encryption: "--disable-encryption-cookies + --password-store=basic 已确认生效，cookie 是明文，问题不在加密层"
  - layer_5_headless_detection: "不排除元宝侧 headless 特征检测导致静默拒绝写入登录 token"

attempted_fixes_and_why_failed:
  - fix: "--disable-encryption-cookies"
    verdict: "已生效 (cookie 可明文读)，但 cookie 内容本身是空的——无 token 可加密"
  - fix: "--password-store=basic"
    verdict: "无效果；Linux 下 Chromium 默认即 basic"
  - fix: "清理 profile 重新扫码"
    verdict: "结果一致：扫码成功但 cookie 无 token"
  - fix: "pkill 所有 chromium 再试"
    verdict: "结果一致"
  - fix: "launch_persistent_context 切换为 launch + new_context + storage_state"
    verdict: "未尝试（root cause 是 cookie 内容，不是持久化方式）"

dead_end_reasons:
  - reason_1: "元宝 QR 登录依赖微信 OAuth iframe 跨域 redirect 链——headless 浏览器对此类流程支持不稳定"
  - reason_2: "即使突破 cookie，元宝可能定期用 refresh token 续签——headless 无法处理过期重登"
  - reason_3: "多用户共用单一服务器 profile = 一人登录覆盖另一人 = 不可行"
  - reason_4: "用户践行零 token 理念，拒绝 API key 方案"

viable_zero_token_path:
  A_local_browser_proxy:
    approach: "用户本地跑 GUI 浏览器代理 (yuanbao_local_proxy.py)，网站改写请求走本地已登录浏览器"
    code: "station/yuanbao_local_proxy.py"
    tradeoff: "改写不再是全自动服务端操作；用户需本地启动代理"
    risk: "未经充分测试"
  B_server_gui_vnc:
    approach: "服务器装 X11 + VNC，跑有 GUI Chromium 扫码"
    tradeoff: "运维复杂；多用户 session 隔离困难；用户未选"
  C_not_selected: "API key 方案被用户拒绝"

user_decision: "交其他 AI 继续尝试；本会话不再继续服务器端元宝登录方向"

historical_lessons:
  - issue: "假定 headless 浏览器 QR 扫码登录与 GUI 浏览器行为等价"
    falsified: "完整 E2E 实测：扫码成功但 auth cookie 缺失"
    solution: "OAuth iframe + redirect 链的登录流程不应假设在 headless 下完整可用；必须先 E2E 验证 cookie token 存在性再继续后续开发"
  - issue: "以 'cookies 文件存在' 为登录成功判据"
    falsified: "cookies 文件存在且非空，但内容全是匿名指纹，无 auth token"
    solution: "判登录成功的唯一可靠信号 = 新 session 打开目标页面不会被重定向到登录页（行为验证，非存储验证）"
  - issue: "未考虑跨域 iframe 内 cookie 写入在 headless 下的限制"
    solution: "涉及第三方 OAuth（微信/QQ/微博等）iframe 扫码登录的场景，headless 浏览器不是可靠方案"
```
