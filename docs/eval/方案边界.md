# Workflow Topology & Constraints

```yaml
task_id: "VU-P1-EXTENSION-STABILIZATION"
goal: "stabilize browser extension full-chain, fix download route, add skip-phash, provide output recovery"
scope_in:
  - "server/mcp_server.py: /local/download route, list_outputs tool, skip_phash in dedup_video tool"
  - "server/pipeline.py: skip_phash parameter, list_outputs() function"
  - "extension/popup.js: skip_phash checkbox logic, phash skipped display, doRecoverOutputs"
  - "extension/popup.html: #chk-skip-phash, #btn-recover-outputs"
  - "docs/{history,eval}: update to current state"
  - "docs/05-扩展功能: archive add-browser-extension change"
  # ---- P3（2026-08-13 起，桌面端架构演进）----
  - "station/desktop/lib/local-server.js: npm start 自动拉起本地 venv mcp_server.py @127.0.0.1:8765"
  - "station/server/yuanbao_client.py: 系统 Edge CDP 驱动元宝（持久调试 profile + 动态端口 + 登录态复制 + 图片上传 + 提示词发送）"
  - "station/desktop/lib/yuanbao-window.js: run-rewrite IPC → spawn venv python yuanbao_client.py --rewrite"
  - "archive/web/app.js: btn-rewrite-preview → extract_copy_context 取 raw_text/frames_b64 再触发改写"
scope_out:
  - "PRD/SAD semantic changes"
  - "hooks/common.py tier evaluator (flat fields only)"
  - "metrics.py threshold changes"
  - "batch_fission expansion"

definition_of_done:
  - "/local/download/<filename> returns 200 with correct Content-Type and Content-Disposition"
  - "dedup_video(skip_phash=True) skips M.compare_videos, returns phash.skipped=true"
  - "popup checkbox defaults to skip_phash=true, showDedupResult shows pHash as neutral '—'"
  - "list_outputs MCP tool returns output dir files with name/size_mb/mtime"
  - "popup btn-recover-outputs calls list_outputs and renders download buttons"
  - "all server changes deployed to 124.71.209.36, service restarted and verified"
  - "docs/history/ + docs/eval/ updated per INDEX.md format"

tech_stack_locked:
  runtime: "python stdlib + vanilla JS + ffmpeg subprocess"
  deploy_method: "paramiko SFTP → systemctl restart video-dedup"
  extension_proto: "MV3 Service Worker background.js"
  forbidden: [Hono, Prisma, BetterAuth, SQL, shell_string_concat]

contract_invariants:
  mcp_proto: "2026-07-28, server/discover replaces initialize"
  human_gate: "resultType=input_required + HMAC-bound requestState"
  delete_output: "BLOCKED"
  dedup_tier: "audit (MUST NOT revert to warned)"

tool_permissions:
  allow: [read_file, edit_file, direct_curl, paramiko_sftp, ffmpeg_subprocess]
  deny: [shell_pipe_concat_ffmpeg, rm_rf_dev2, drop_output_dir, manual_delete_storage_without_backup]

runtime_limits:
  ffmpeg_timeout_seconds: 600
  dedup_expected_duration: "1-2min (skip_phash=true), 4-5min (skip_phash=false)"

esc_trigger:
  on_tier_revert: HUMAN_FIX_IMMEDIATELY  # dedup_video._tier went from audit→warned
  on_download_404: CHECK_SERVE_WEB_ROUTE
  on_env_var_drift: VERIFY_SYSTEMD_ENVIRONMENT
  on_tts_401: EXPOSE_IN_UI_WITH_DOWNGRADE
  on_prd_semantic_gap: RECORD_IN_沉淀失败原因_THEN_ASK
  # ---- P3 桌面端约束（2026-08-13 起）----
  on_remote_server_used: FORBIDDEN            # 桌面端禁止依赖远端 124.71.209.36，必须本地 127.0.0.1:8765
  on_yuanbao_builtin_window: FORBIDDEN       # 元宝改写禁止 Electron 内置 BrowserWindow，必须系统 Edge CDP
  on_debug_port_hardcoded: USE_FREE_PORT      # 调试端口禁止写死 9223，用动态空闲端口
  on_edge_not_closed_for_copy: CLOSE_EDGE_ONCE  # 复制登录态前必须 taskkill 关一次 Edge
```
