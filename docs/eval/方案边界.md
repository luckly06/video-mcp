# Workflow Topology & Constraints

```yaml
task_id: "VU-P0-DD-CLOSEOUT"
goal: "close DD F1.1-F5.1 implementation, regression assets and browser QA handoff without overstating independent acceptance"
scope_in:
  - "server/pipeline.py: runtime contract, requestState/hook/origin security and batch_fission delivery gate/default=5"
  - "tests/: unit + real-ffmpeg lightweight integration regression"
  - "web/: controls/rendering plus stable manual checklist"
  - "docs/{02-方案设计,eval,history}: synchronize evidence and lifecycle"
scope_out:
  - "PRD/SAD semantic changes"
  - "hooks/common.py tier evaluator (flat fields only)"
  - "CRVideoMate/ GUI archive (reference only, never modify)"
  - "independent QA signoff (must be performed by an independent reviewer)"

definition_of_done:
  - "F1.1-F5.1 implementation and tests are green"
  - "batch_fission default is 5 across pipeline/rules/Web/DD"
  - "station/web/README-手测清单.md exists and covers steps 1-7"
  - "requestState binds the confirmed name+arguments; PreToolUse failures fail closed; only local Host/Origin is accepted"
  - "dedup and fission expose explicit delivery gates and Web never reports failed gates as deliverable"
  - "server smoke job handles are isolated from station/logs/jobs.json"
  - "implementation-side browser report remains distinct from independent QA signoff"
  - "pytest tests/ -q, Python compile, app.js syntax and git diff --check pass"
  - "lifecycle remains WAITING_HUMAN until independent QA signs"

tech_stack_locked:            # PRD/DD actual, NOT template defaults
  runtime: "python stdlib + vanilla JS + ffmpeg subprocess"
  deploy: "local single-user, no DB / no login / no cookie"
  forbidden: [Hono, Prisma, BetterAuth, SQL, shell_string_concat]

contract_invariants:
  mcp_proto: "2026-07-28, server/discover replaces initialize"
  human_gate: "resultType=input_required + HMAC-bound requestState(name+arguments)"
  confirm_field: "params.inputResponses.confirm=true and params.requestState (siblings of name/arguments)"
  normal_return: '{"content":[...],"isError":false}  # no resultType key'
  delete_output: "BLOCKED hard, even with human confirm"

tool_permissions:
  allow: [read_file, edit_file, glob, grep, run_pytest, ffmpeg_subprocess_list_argv]
  deny: [shell_pipe_concat_ffmpeg, rm_rf_dev2, git_clean_fd, drop_output_dir]

runtime_limits:
  pytest_wall_seconds: 180
  ffmpeg_timeout_seconds: 600
  phash_sample_frames: 16

esc_trigger:
  on_threshold_change: HUMAN_AUTHORIZE_FIRST   # PRD 16.2 says const-only
  on_prd_semantic_gap: RECORD_IN_沉淀失败原因_THEN_ASK
  on_conclusion_without_data: RERUN_EXPERIMENT_BEFORE_ASSERT
```

---

## Concurrent task: extension voiceover chain

```yaml
task_id: "VU-EXT-配音链"
goal: "ship subtitle/ASR text extraction + DeepSeek rewrite + MiMo TTS audio replacement on top of frozen P0"
parent_constraint: "SAD frozen; delta may only EXTEND DD; PRD/SAD read-only (05-扩展功能 README §2)"
scope_in:
  - "server/{tts_client,asr_client,copy_rewriter}.py"
  - "server/pipeline.py: TTS segment inside dedup_video only"
  - "web/: voice/speed/template controls + tts_process report line"
  - "docs/05-扩展功能/changes/{add-tts-audio-replace,add-asr-copy-rewrite}/"
scope_out:
  - "P0 dedup dimensions / metrics caliber / MCP protocol"
  - "generative model inside the dedup core (PRD §10: dedup intelligence stays deterministic)"
  - "auto-committing or pushing without explicit human ask"

definition_of_done:
  - "subtitle -> ASR -> rewrite -> TTS degrades stepwise; each miss records a reason, never blocks dedup"
  - "rewrite failure falls back to raw text and surfaces reason via get_last_error() -> applied.rewrite_error"
  - "browser automation never borrows the user's daily Edge/Chrome User Data dir"
  - "headless automation exposes no HeadlessChrome UA to the target site"
  - "text handed to tts_client carries no newline / markdown / prefix residue"
  - "--probe prints a real rewritten line with exit=0 (requires human --login first)"

browser_automation_constraints:      # 2026-08-10 measured, do not re-litigate
  profile: "dedicated PROFILE_DIR only; VU_DEEPSEEK_PROFILE overrides; user_data_dir of running browser => TargetClosedError => about:blank"
  login_state_signal: "redirect to /sign_in is the ONLY reliable check; cookie-name allowlists are falsified"
  ua: "read real UA at launch, strip Headless marker; never hardcode version"
  dom_source_of_truth: "afumu/openteam src/content/sites/deepseek.ts (+ domText / waitForElement / responseContainers / replyObserver)"
  injection: "textarea value + InputEvent; contenteditable path is wrong for DeepSeek"
  completion: "text stable >=1.5s (short reply 5s) AND no stop button; partial text on still-generating timeout must be DISCARDED"

tool_permissions:
  allow: [read_file, edit_file, glob, grep, run_pytest, playwright_dedicated_profile, ffmpeg_subprocess_list_argv]
  deny:
    - playwright_on_user_daily_profile     # single-instance lock => about:blank
    - headless_default_ua_to_waf_site      # HeadlessChrome marker
    - commit_or_push_without_human_ask
    - hardcode_absolute_model_or_profile_path   # use VU_* env + __file__ anchor

runtime_limits:
  rewrite_reply_timeout_seconds: 60
  login_wait_seconds: 300
  send_button_wait_seconds: 15

esc_trigger:
  on_selector_stale: REPIN_FROM_LIVE_DOM_THEN_RECORD   # DeepSeek re-skin breaks .bf38813a/._52c986b
  on_login_required: STOP_AND_ASK_HUMAN                # AI cannot scan a QR code
  on_rate_limit_429: BACKOFF_AND_REPORT_NOT_RETRY_BLIND
  on_truncated_reply: FAIL_AND_FALLBACK_TO_RAW_TEXT     # never voice a half sentence
```
