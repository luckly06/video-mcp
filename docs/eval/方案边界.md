# Workflow Topology & Constraints

```yaml
task_id: "VU-P0-F2.4"
goal: "close variant-vs-variant separation gap in batch_fission; DD AppxB F2.4"
scope_in:
  - "server/pipeline.py: _resolve_level / _calc_trim variant spread"
  - "server/metrics.py: threshold semantics (avg + weak_frame_ratio)"
  - "tests/: lock semantics as regression"
scope_out:
  - "hooks/common.py tier evaluator (flat fields only, no nested dimensions.flip)"
  - "F3.x server param passthrough / F4.x web render (later phases)"
  - "CRVideoMate/ GUI archive (reference only, never modify)"

definition_of_done:
  - "batch_fission on 8.0s + 49.5s + 5.1s assets: matrix.all_pass=true"
  - "variant-vs-source passed stays true (no threshold relaxation)"
  - "pytest tests/ -q all green (baseline 41)"
  - "no temp artifacts left in output/ or station/"

tech_stack_locked:            # PRD/DD actual, NOT template defaults
  runtime: "python stdlib + vanilla JS + ffmpeg subprocess"
  deploy: "local single-user, no DB / no login / no cookie"
  forbidden: [Hono, Prisma, BetterAuth, SQL, shell_string_concat]

contract_invariants:
  mcp_proto: "2026-07-28, server/discover replaces initialize"
  human_gate: "resultType=input_required + requestState"
  confirm_field: "params.inputResponses.confirm=true (sibling of name/arguments)"
  normal_return: '{"content":[...],"isError":false}  # no resultType key'
  delete_output: "BLOCKED hard, even with human confirm"

tool_permissions:
  allow: [read_file, edit_file, glob, grep, run_pytest, ffmpeg_subprocess_list_argv]
  deny: [shell_pipe_concat_ffmpeg, rm_rf_dev2, git_clean_fd, drop_output_dir]

runtime_limits:
  pytest_wall_seconds: 30
  ffmpeg_timeout_seconds: 600
  phash_sample_frames: 16

esc_trigger:
  on_threshold_change: HUMAN_AUTHORIZE_FIRST   # PRD 16.2 says const-only
  on_prd_semantic_gap: RECORD_IN_沉淀失败原因_THEN_ASK
  on_conclusion_without_data: RERUN_EXPERIMENT_BEFORE_ASSERT
```
