# Session Runtime Snapshot

```yaml
snapshot_timestamp: 2026-08-04T23:35:29+08:00
lifecycle_status: WAITING_HUMAN   # implementation and closeout regression done; independent QA signoff pending
task_id: "VU-P0-DD-CLOSEOUT"

execution_stack:
  current_node: independent_qa
  parent_node: critic
  depth: 2

progress_dd_appendix_b:
  F1.1_pair_metric: DONE
  F1.2_matrix_fallback: DONE
  F2.1_safety_levels: DONE
  F2.2_crop_flip: DONE
  F2.3_speed_trim: DONE
  F2.4_selfcheck_fission: DONE
  F3.1_rules_json: DONE
  F3.2_server_params: DONE
  F3.3_hook_whitelist: DONE
  F4.1_web_controls: DONE
  F4.2_phash_render: DONE
  F5.1_skill_sop: DONE

quality_metrics:
  success: null                   # formal delivery remains open until independent QA report exists
  context_health: OK
  human_takeover_count: 1
  note: "implementation, 110-test runtime regression and implementation-side browser steps 5-7 passed; independent QA signoff remains pending"

# ---- measured: per-dimension contribution to VARIANT-vs-VARIANT distance ----
# n=16 frames, ratio-normalized sampling, gate = avg>=12 AND weak_ratio<=0.10
dimension_separation_power:
  speed_1.10_vs_0.90:   {avg: 1.875, weak: 1.000, pass: false, verdict: NULL_OP}
  rotate_-1_vs_+1:      {avg: 4.12,  weak: 0.812, pass: false, verdict: TOO_WEAK}
  crop_0.02_vs_0.08:    {avg: 7.50,  weak: 0.438, pass: false, verdict: TOO_WEAK}
  time_offset_1.0s:     {avg: 29.12, weak: 0.062, pass: true,  verdict: EFFECTIVE}
  flip_h_vs_v:          {avg: 33.00, weak: 0.000, pass: true,  verdict: EFFECTIVE}
key_finding: >
  speed is CANCELLED BY the metric itself: both videos sample at relative
  instant (k+0.5)/n on their own timeline, so a pure duration change maps back
  to identical content. Not a bug - inherent to ratio-normalized sampling.
  Only 2 usable axes for inter-variant spread: TIME OFFSET and FLIP.

# ---- FIX LANDED (F2.4-01) ----
fix_implemented:
  mechanism: "deterministic trim-phase spread, replaces iid sampling per variant"
  code:
    - "_calc_trim(duration, band, seed, phase=None): phase in [0,1] fixes TOTAL trim
       budget and spreads the head/tail SPLIT linearly. budget = min(2*hi,
       duration*MAX_TRIM_RATIO, duration-MIN_DURATION_TRIM); head=budget*phase."
    - "dedup_video(..., trim_phase=None) threads it through; records applied_params.trim_phase"
    - "batch_fission: phase = i/(count-1), so head span fills the whole legal window"
  invariants_preserved:
    - "phase=None keeps the original iid behaviour (all 41 pre-existing tests untouched)"
    - "out_dur identical across variants (budget fixed) -> duration_close self-check unaffected"
    - "all three MIN_DURATION guards still bind; phase-mode zero-window returns skipped, not silent 0-trim"

verification_after_fix:
  asset_8.0s_剑鬼:                # the reproduced failing case
    trim_budget_s: 0.800          # 10% clamp; adjacent head delta = 0.400s
    heads: [0.0, 0.4, 0.8]
    vs_source: [20.00, 19.25, 23.375]      # all pass
    pairwise: {v1v2: 17.375, v1v3: 23.875, v2v3: 18.50}
    all_pass: true                # was false; worst pair 3.50 -> 18.50
  asset_5.06s_short:
    time_leg: absent              # duration < MIN_DURATION_TRIM=7.0 -> trim skipped by design
    time_only_pairwise: {v1v2: 1.625, v1v3: 0.50, v2v3: 1.75, all_pass: false}
    flip_spread_pairwise: {v1v2: 32.25, v1v3: 35.875, v2v3: 28.75, all_pass: true}
    conclusion: "for sub-7s material distinct flip_mode per variant is the ONLY lever"
  tests: "48 passed ~26s (41 pre-existing + 7 new phase-semantics tests)"

falsified_by_this_verification:                 # prior session's own open risk
  claim: "8.0s asset budget 0.8s may be geometrically unable to reach delta 1.0s"
  outcome: FALSIFIED
  why: >
    the ~1.0s delta figure was measured on the 49.5s asset. Required delta is
    CONTENT-MOTION dependent, not universal: on 剑鬼 8.0s a 0.400s adjacent
    delta already yields avg 17.4-23.9. Do not treat 1.0s as a global constant.

separation_diagnostic:            # added so a failing matrix names the missing leg
  field: "batch_fission() -> separation"
  keys: {time_leg: present|absent, flip_spread: bool, hint: str}
  behaviour: "hint only emitted when matrix.all_pass is false and flip_spread is false"
  verified: "fires correctly on a synthetic 5.0s clip (time_leg=absent + flip hint)"

deliverables_index:
  implementation:
    - "F1.1-F5.1 landed; F5.1 core index: station/shared/skills/SKILL.md"
    - "DD closeout: batch_fission default=5 in pipeline/rules/Web/DD"
    - "acceptance assets: station/web/README-手测清单.md + tests/test_pipeline_e2e.py"
    - "runtime regression: 112 passed in 27.99s; Python compile / app.js syntax passed; final git diff --check passed"
    - "requestState HMAC binds original name+arguments; missing/tampered/mismatched confirmation is rejected before execution"
    - "PreToolUse hook failures fail closed; non-local Host/Origin is rejected; CORS wildcard removed"
    - "delivery gates: dedup checks.all_passed; fission delivery_ready=all_unique && matrix.all_pass"
    - "server smoke jobs.json isolated to pytest tmp_path; station/logs/jobs.json unchanged"
    - "Q-03 objective sync: synthetic 31s beat track, heavy speed_factor=1.10, duration_close=true, audio-video stream delta<=0.10s"
    - "pytest sandbox could not recycle its own temp garbage directories; tests still passed and project output was not polluted"
  browser_qa:
    steps_1_4: PASS              # existing manual result
    steps_5_7_execution: PASS    # 2026-08-04 browser execution; see F5.1-浏览器步骤5-7执行报告.md
    independent_qa_signoff: PENDING
    independent_qa_attempt: "BLOCKED: independent runner launch returned platform 403 on 2026-08-04; no test execution occurred"
    phash_path: "signature fallback; numeric phash threshold not applied"
    rule: "implementation-side execution is not independent QA signoff; a QA member failure is never a pass"
  temp_artifacts: CLEANED
  residual_output: [剑鬼_变体1-3.mp4, 微笑_变体1-2.mp4, 微笑_去重.mp4, 下班来接我_去重.mp4]

residual_validation:
  Q02_platform_effect: "PENDING post-distribution manual feedback; engineering constants remain frozen"
  Q03_objective_av_sync: "DONE: 31s synthetic beat-track heavy 1.10x test passed; audio-video stream delta<=0.10s"
  Q03_subjective_music_quality: "MANUAL_OBSERVATION_ONLY; not a code/release gate"

next_action:
  - "independent QA member uses station/web/README-手测清单.md to review or rerun steps 1-7 and signs the report"
  - "only after independent signoff may lifecycle_status move from WAITING_HUMAN to SUCCESS"
```
