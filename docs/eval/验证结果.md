# Session Runtime Snapshot

```yaml
snapshot_timestamp: 2026-08-12T16:29:53+08:00
lifecycle_status: ACTIVE   # extension full-chain functional, stability closing
task_id: "VU-P1-EXTENSION-STABILIZATION"

execution_stack:
  current_node: final_verification
  parent_node: fixes_landed
  depth: 2

progress_extension:
  X1_skeleton: DONE
  X2_rewrite: DONE
  X3_tts: DONE
  X4_download_route: DONE
  X5_skip_phash: DONE
  X6_output_recovery: DONE

quality_metrics:
  success: true                    # all fixes deployed and verified
  context_health: OK
  human_takeover_count: 2          # tier override diagnosis + key rotation
  note: "3 critical bugs fixed: /local/download route missing, pHash 3-5min bottleneck, no recovery after storage clear"

# ---- Fix verification ----
fix_01_download_route:
  test: "curl -I localhost:8765/local/download/2_%E5%8E%BB%E9%87%8D_6.mp4"
  result: "HTTP/1.0 200, Content-Type: video/mp4, Content-Disposition: attachment"
  status: PASS

fix_02_skip_phash:
  test: "dedup_video called with skip_phash=true → no M.compare_videos CPU spike"
  pipeline: "phash={skipped:true, passed:null, method:'skipped'}, all_passed=(md5_changed && resolution_kept && duration_close && min_duration_ok)"
  ui: "pHash check-item shows neutral '—' with label 'pHash (已跳过)'"
  status: DEPLOYED_AWAITING_LIVE_TEST

fix_03_output_recovery:
  test: "MCP tools/call list_outputs → JSON with name/size_mb/mtime for all output files"
  result: "6 files returned, sorted by mtime desc"
  popup: "btn-recover-outputs → list_outputs → download buttons"
  status: PASS

# ---- Existing acceptance (v1.0) ----
extension_v1_acceptance:
  upload: PASS           # XHR + FormData, progress bar, >64MB OK
  probe: PASS            # ffprobe via MCP, all fields shown
  rewrite: PASS          # content-yuanbao.js → 元宝 tab → capture → badge OK
  dedup: PASS            # level + dimensions + orb-loader + timer + badge
  tts: PASS              # MiMo key2 valid, 401 exposed in UI, pink hint
  download: NOW_FIXED    # was 404 (route missing); now 200 OK
  recovery: NEW          # list_outputs + btn-recover-outputs

deliverables_index:
  server:
    - "mcp_server.py: /local/download route + list_outputs tool + skip_phash passthrough"
    - "pipeline.py: skip_phash param + list_outputs() function"
  extension:
    - "popup.html: #chk-skip-phash (default checked) + #btn-recover-outputs"
    - "popup.js: skip_phash in doDedup + neutral pHash display + doRecoverOutputs()"
  docs:
    - "docs/history/开发过程.md: entries for 2026-08-09/11/12"
    - "docs/history/当前状态.md: full rewrite to P1 extension architecture"
    - "docs/eval/方案边界.md: updated for extension stabilization"
    - "docs/eval/验证结果.md: this file"
    - "docs/eval/沉淀失败原因.md: 3 new recurring failure entries"
    - "docs/05-扩展功能/changes/add-browser-extension: archived"
    - ".cursorrules/未完成功能.md: updated"

residual_output:
  server: "/opt/video-dedup/output/2_去重_6.mp4 (104MB) + 6 earlier variants"
  download_link: "http://124.71.209.36:8765/local/download/2_去重_6.mp4"
```

```yaml
# ---- 2026-08-13/14 P3 验证（本地后端 + 系统 Edge CDP 驱动元宝）----
task_id: "VU-P3-EDGE-CDP-YUANBAO"
updated: 2026-08-14T11:07:00+08:00
lifecycle_status: ACTIVE
node: D8_edge_cdp_yuanbao

local_backend:
  test: "station/desktop npm start 自动 spawn venv mcp_server.py @127.0.0.1:8765"
  tool: "extract_copy_context(input/剑鬼.mp4)"
  result: "raw_text(ASR, source=asr) + 5 张 frames_b64 JPEG 返回正常"
  status: PASS

edge_cdp_launch_and_navigate:
  what: "系统 Edge 经 CDP（动态端口 + 持久调试 profile）启动并打开元宝"
  test: "ensure_edge_debug_port() → 启动调试 Edge → connect_over_cdp → 选元宝页"
  sandbox_result: "CDP 连上→自动进 yuanbao.tencent.com/chat/*→正确识别登录弹窗"
  user_result: "用户本机实测：已成功跳转元宝"
  status: PASS

yuanbao_prompt_send:
  what: "把提示词（文案 + 视频截图）填入元宝并发送"
  root_cause: "REWRITE_TEMPLATE 与已验证的 content-yuanbao.js 实现不一致：fill() 填 contenteditable 编辑器失效；图片上传依赖 expect_file_chooser（CDP 调试模式下常不触发→超时）"
  fix_1: "对齐 content-yuanbao.js：contenteditable 用 innerText+dispatch input 填充；图片 base64 解码落盘后直接 set_input_files 到 input[type=file].first；Enter+发送按钮兜底（commit 03b4596）"
  mech_verify: "沙箱本地复刻 DOM 验证通过：editor 命中、innerText 填充成功、files=1、发送按钮触发 SENT"
  residual_bug: "fix_1 后日志显示『已 set_input_files 3 张 + 缩略图出现:True』，但用户反馈『提示词仍没图』。根因：set_input_files 只灌了 input[type=file].first，那是元宝的『别的』上传入口，预览出现了但随消息真正发出的图片 input 没拿到文件 → 静默失败。"
  final_fix: "复刻 content-yuanbao.js 的 uploadImages 正确 UI 路径：先点『+』上传图标容器，再点『图片』菜单项挂载真正图片 input，再用 page.expect_file_chooser + chooser.set_files 受信注入（不弹系统对话框）。找不到『图片』/未触发选择框时退化 set_input_files 到 .first。仅删临时解码帧，不动真实帧路径。"
  user_result: "用户本机实测（2026-08-14 11:07）确认：改写链路图片已随提示词成功发给元宝（无弹窗、图片真正带上）"
  status: PASS

edge_login_persist:
  what: "复制的登录态在持久调试 profile 能否被 ABE 解密（决定元宝是否真登录）"
  result: "用户实测全流程成功，登录态复制生效、未触发 ABE 拦截"
  fallback: "若个别机器遇到 ABE 拦截（弹登录框），用户在窗口内扫码一次（profile 持久，后续免登录）"
  status: PASS

summary:
  passed: [local_backend, edge_cdp_launch_and_navigate, yuanbao_prompt_send, edge_login_persist]
  blocked: []
  next: "改写→TTS 自动链路（mcp_server.dedup_video 接 vision_and_rewrite→TTS 混音）已随本次图片修复一并在用户本机验证；剩余：归档 changes/add-desktop-electron、electron-builder 打包、裂变 batch_fission 接改写→TTS"
```

```yaml
# ---- 2026-08-14 下午 P3 打磨（UI/链路修复，已推 dev）----
task_id: "VU-P3-DESKTOP-POLISH"
updated: 2026-08-14T12:23:00+08:00
lifecycle_status: ACTIVE
node: desktop_polish

ui_orb_loader:
  what: "去重/裂变『开始』按钮的 orb-loader(紫色滚动球) 不显示"
  root_cause: "startProgress 写在 callToolWithConfirm 的 onExecute 回调里，首次调用直接返回结果时回调不触发"
  fix: "startProgress + 取消按钮状态 提到 callToolWithConfirm 之前（去重与裂变按钮均适用）"
  status: PASS

ui_rewrite_race:
  what: "AI 改写预览失败却加载了上一次的旧文案"
  root_cause: "yuanbao-done 异步事件无 request_id 匹配，多路改写串台"
  fix: "yuanbao-window.js 每次生成 request_id 原样回传；app.js 按 request_id 匹配 + _rewriteInFlight 守卫 + 180s 超时兜底 + 失败/开始清空 rewritten 标记"
  status: PASS

tts_meta_clean:
  what: "元宝输出『…注：文案共136字…』会被 TTS 念出"
  root_cause: "SYSTEM_PROMPT 诱导元宝自报字数/合规确认；无防御清洗"
  fix: "copy_rewriter 强化 SYSTEM_PROMPT 禁用元信息 + 新增 _strip_tts_meta 防御清洗剔除『注：』尾巴；mcp_server 喂 tts_text 前再兜底一次"
  status: PASS

duration_judge_fix:
  what: "时长合规却显示红色『变化超出允许范围（ΔXs）』"
  root_cause: "tts_duration 探测失败(None)→后端 fallback 用原视频时长作基准→成片(=配音)比源短被判 duration_close=False；前端 ttsOn 因缺 tts_duration 走非 TTS 分支"
  fix: "pipeline.py：TTS 生效用成片时长(-shortest=配音)兜底 tts_duration 作基准并回填；app.js 放宽 ttsOn 触发(tts_applied 即真)；TTS 生效且成片更短时显示中性绿色『TTS 输出全部文案，但视频时长缩短了 Xs（配音 Ys）』"
  status: PASS

tts_mode_rainbow:
  what: "TTS 模式二选一按钮（自动提取+AI改写 / 手动输入文案）选中态无明确标识"
  fix: "选中态复用输入框聚焦彩虹边框(#e8429b→#9b5be8→#3fc7e8→#b8f25f→#f5b946)+紫色光晕"
  status: PASS

sidebar_collapse:
  what: "右侧边栏【验收记录】【工具与分级】两个面板过长、占版面"
  fix: "两个标题栏各加折叠按钮（SVG 箭头），点一下收起/展开面板内容，状态存 aria-expanded"
  status: PASS

summary:
  passed: [ui_orb_loader, ui_rewrite_race, tts_meta_clean, duration_judge_fix, tts_mode_rainbow, sidebar_collapse]
  commits: [30a1b7e, 50d9f26, cbf95eb, 7534b41]
  next: "归档 changes/add-desktop-electron；electron-builder 打包；裂变 batch_fission 接改写→TTS"
```

```yaml
# ---- 2026-08-14 P4 桌面端产物目录与侧栏收口 ----
task_id: "VU-P4-DESKTOP-OUTPUT-DIRS"
updated: 2026-08-14T08:56:00+08:00
lifecycle_status: ACTIVE
node: desktop_output_directory_closeout
current_line_notice: "当前项目已更改为桌面端；web 端、扩展插件与远端 124.71.209.36 均为旧版/历史参考"

output_directory_config:
  what: "去重产物文件夹与裂变产物文件夹分别配置、分别打开"
  implementation:
    frontend: "archive/web/app.js loadOutputDirs/configureOutputDir/ensureOutputDir(kind)"
    backend: "mcp_server.py /local/get-output-dir /local/set-output-dir"
    config: "~/.video-uniqueness/config.json: dedup_output_dir / fission_output_dir"
  expected: "首次未配置时，开始去重或裂变会弹系统文件夹选择；配置后【产物目录】高亮消失"
  status: PASS_STATIC_CHECK

asset_workspace_layout:
  what: "产物按原视频聚合到素材工作区，避免根目录平铺难管理"
  rule: "{产物根目录}/{原视频名}__{源视频MD5前8位}/"
  implementation: "mcp_server.py _asset_workspace_dir(src, root_dir) + pipeline subdir=''"
  expected: "同一素材的去重/裂变多次产物落在同一素材工作区；不同同名文件用 MD5 前8位区分"
  status: PASS_STATIC_CHECK

download_and_open_safety:
  what: "前端展示文件名但内部保留工作区相对路径；下载/打开按 subdir 定位去重或裂变根目录"
  route: "/local/download/<relative_path>?subdir=去重|裂变"
  guard: "Path(relative_path).is_absolute() 拒绝；resolve 后必须 relative_to(对应产物根目录)"
  status: PASS_STATIC_CHECK

sidebar_collapse:
  what: "顶部一键收纳左右侧栏 + 右侧单独收纳状态共存"
  fix: ".workspace-shell:not(.sidebars-collapsed):has(.audit-sidebar.panel-collapsed) 避免整体收纳时仍保留左侧 224px 空白；顶部展开时清右侧 panel-collapsed"
  status: PASS_STATIC_CHECK

validation_plan:
  static:
    - "PASS: node --check archive/web/app.js"
    - "PASS: python -m py_compile station/server/mcp_server.py station/server/pipeline.py"
    - "PASS: git diff --check"
  manual_required:
    - "Electron GUI：目录选择对话框、打开文件夹、真实下载与侧栏交互需用户本机验收"
  note: "Agent 沙箱不能验证 Electron GUI；仅能做静态语法与路径逻辑检查"
```

```yaml
# ---- 2026-08-21 P4 普通用户电脑自包含运行时验证 ----
- id: V44_bundled_python_runtime
  result: PASS
  evidence:
    - "npm test 8/8；覆盖 Python 解析优先级与既有 MCP 端口回退"
    - "station/vendor/python/python.exe 可导入 playwright.async_api"
    - "随包 Python 独立启动 mcp_server.py 后，POST /mcp server/discover 返回 HTTP 200"
    - "dist/win-unpacked 冒烟日志显示实际使用 resources/station/vendor/python/python.exe，MCP 在 127.0.0.1:8765 就绪"
    - "发布 ZIP 7-Zip 完整性测试通过；182.72 MiB；SHA-256 856499150CE2C67BCA27EA9C5D76B7E652999EDE091EA1C7221E8925DD0A4012"
  fixed_failure: "普通用户未安装 Python 时，旧版 spawn python 返回 ENOENT，前端仅显示无法连接 MCP Server"
  boundary:
    - "仍要求 Windows x64、完整解压 ZIP，且 Windows 安全中心未隔离 exe/dll/pyd"
    - "离线 ASR 模型仍为可选外部资源；缺失只降级语音识别，不阻塞去重与 MCP 启动"
```

```yaml
# ---- 2026-08-15 P4 元宝隔离与 TTS 显式闸门验证 ----
task_id: "VU-P4-YUANBAO-TTS-GATES"
updated: 2026-08-15T00:00:00+08:00
lifecycle_status: ACTIVE
node: yuanbao_rewrite_isolation_and_tts_gate

rewrite_request_isolation:
  what: "AI 改写预览多轮请求不能串台，旧视频结果不能覆盖新视频预览"
  implementation: "yuanbao request_id 严格匹配；过期/不匹配回包忽略"
  expected: "连续切换素材并多次改写时，UI 只接收当前请求的元宝结果"
  status: PASS_STATIC_CHECK

no_user_edge_kill:
  what: "元宝扫码/改写禁止关闭用户正在使用的系统 Edge"
  implementation: "扫码与改写统一走 userData 下独立 debug profile；桌面端只管理自己拉起的调试 Edge"
  expected: "用户正常浏览的 Edge 不被关闭；首次扫码一次后调试 profile 复用登录态"
  status: PASS_STATIC_CHECK

tts_enable_explicit_gate:
  what: "默认不 TTS 配音，只有单条去重 + 用户显式勾选启用 AI 配音才消费 TTS 参数"
  implementation:
    frontend: "archive/web/index.html 新增 chk-tts-enable；archive/web/app.js readTTS 先检查该勾选项"
    backend: "station/server/mcp_server.py schema 新增 enable_tts，且仅 enable_tts=true 时消费 tts_text/rewrite_template"
  expected:
    - "未勾选 AI 配音：单条去重不读取/不提交 TTS 文案"
    - "批量裂变：不 readTTS、不透传 TTS"
    - "AI 改写预览：只预览文案；确认使用文案后才自动勾选 AI 配音"
    - "TTS 未生效提醒：只在单条去重 + 显式勾选 + 配音未生效时出现"
  status: PASS_STATIC_CHECK

fission_no_tts_passthrough:
  what: "批量裂变当前不支持 TTS，不读取、不透传 TTS 参数"
  implementation: "doFission 不调用 readTTS；后端裂变链路不消费 tts_text/rewrite_template"

# ---- 2026-08-19 P4 验证（发布态 MiMo TTS 配置）----

- id: V40_packaged_tts_runtime_config
  result: PASS
  evidence:
    - "MiMo models 鉴权成功，返回 6 个模型，确认 MIMO_API_KEY 有效且未过期"
    - "tts_client.py 不导入 openai，使用 urllib 合成『测试』成功返回 53804 bytes，文件头 52494646（RIFF）"
    - "runtime-config.test.js 2/2 通过：可还原 Key、篡改密文会拒绝"
    - "生成的 runtime-config.bin 为 210 bytes，不包含 51 字符明文 Key"
  boundary: "纯本地密钥可被高水平逆向者从程序逻辑中恢复；当前方案目标是避免明文 .env 和小白式直接复制，不等同服务器侧密钥保密"
  expected: "裂变不会因为改写模板或文案输入触发配音，也不会弹 TTS 未生效提醒"
  status: PASS_STATIC_CHECK

validation_plan:
  static:
    - "PASS: node --check archive/web/app.js"
    - "PASS: python -m py_compile station/server/mcp_server.py station/server/yuanbao_client.py"
    - "PASS: git diff --check"
  manual_required:
    - "新包端到端：连续两个视频 AI 改写预览 request_id 串台防护需用户本机验收"
    - "新包端到端：不关闭用户 Edge、首次扫码一次后复用独立 profile、AI 配音显式闸门与裂变不配音需用户本机验收"
  note: "本轮只做静态验证与文档同步；不打包、不提交、不推送。"
```
