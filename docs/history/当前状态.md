# 当前状态（项目快照）

> **维护守则**（承 docs/INDEX.md §1）
> 本文件是**唯一的"现在走到哪了"快照**，切片 1 视角，**每跨阶段整块重写**（不做增量追加，历史流水在 `开发过程.md`）。
> 只写"接手就能干活"所需：位置 / 启动 / 进度 / 下一步 / 红线。数值实验与失败归因不进此文件，指向 `docs/eval/`。

```yaml
snapshot: 2026-08-12T09:42:43+08:00
project: "视频去重 Agent 工位（video-uniqueness / station）"
phase: "P0 已收口待独立 QA；05-扩展功能 浏览器扩展基本可用（改写+去重+下载+TTS密钥已配）"
lifecycle: WAITING_HUMAN            # 扩展: 改写/去重/下载/TTS链路已通, MiMo已配待配音验收；P0 独立 QA 待签

# ---------------- 位置与启动 ----------------
location: "F:/ai_agent/dev2/xiaomi/ai/test11/video-uniqueness/"
layout:
  station/: "工程本体（入口 station/run.py）"
  docs/01-需求分析/02-PRD.md: "业务唯一依据"
  docs/02-方案设计/{01-SAD,02-DD}.md: "开发严格照 DD 附录 B 顺序"
  docs/eval/: "运行时状态机（边界/快照/失败沉淀）"
  docs/history/: "开发流水 + 本快照"
  CRVideoMate/: "GUI 原件归档，纯参照，勿改"
  output/: "产物目录"
run:
  start: "cd F:/ai_agent/dev2/xiaomi/ai/test11/video-uniqueness/station && python run.py"
  test:  "C:/Users/lucky/.workbuddy/binaries/python/envs/default/Scripts/python.exe -m pytest F:/ai_agent/dev2/xiaomi/ai/test11/video-uniqueness/station/tests -q"
  test_result_2026_08_10: "166 passed in 34.47s（复跑 33.69s 一致）  # 旧记载的 112 passed 已过期"
  test_fixed_this_round: >
    本轮修掉 4 项陈旧用例（均为用例未随设计同步，非产品缺陷）：
    (1) test_default_video_dir → VIDEO_DIR 早已是 %TEMP%/vu-uploads（HEAD 即如此，该用例在改动前就红），
        断言改为锚定临时上传目录 + 素材/产物必须异根；
    (2) delivery_ready → 交付门已升为三重（MD5 + pHash + SSIM），用例只 stub 了两门，
        补 ssim_matrix stub 并新增一条 SSIM 拒绝用例（4 组 → 5 组）；
    (3)(4) _StubPipeline.{dedup_video,batch_fission} 签名缺 TTS 四参 → server 透传后抛 TypeError
        导致 stub.calls 为空，补齐签名并记录 tts_* / rewrite_template。
    新增 station/tests/test_copy_rewriter.py（27 项纯函数用例，不起浏览器）。
  login_deepseek: "python station/server/copy_rewriter.py --login   # 首次一次即可；随后 --probe 自证"
  interpreter_note: >
    使用 WorkBuddy 托管 Python 3.13 venv；requirements.txt 已安装在隔离环境。
    直接 import pre_tool_guard 会触发其顶层 sys.stdin/stdout 重写，F3.3 测试继续用 subprocess 隔离。
    当前 110 项测试全部通过；真实 ffmpeg 集成测试与 server smoke 的 jobs.json 均写入 pytest tmp_path，
    不污染项目 output/ 或 station/logs/jobs.json。
warning_stale_path: >
  旧中文路径【不是空壳】（2026-08-04 实地核对，纠正此前记载）：
  (1) 叶子 test11/视频综合处理/视频综合处理/CR综合处理永久版（不要更新）/ 除 .claude/ 外无其他内容，
      但 .claude/settings.local.json（784B，非 8K）存着 15 条 permissions.allow 授权白名单
      （Bash/Read，含 pytest、rm -f 产物、hook 脚本）。本会话 shell 锚在此，删除即失授权。
  (2) 祖先 test11/视频综合处理/ 共 879M，与 station 工程无关但不可连带删除：
      CR视频去重教程（旧）/CR视频去重教程（旧）.mp4 819M、扫光素材.rar 19M、
      视频综合处理/video/{下班来接我.mp4 26M, 下班来接我_去重demo.mp4 17M}。
  每条 bash 后 cwd 会重置回叶子目录 -> 一律用绝对路径操作。

# ---------------- 进度（DD 附录 B）----------------
progress:
  F1.1_pair_metric:        DONE
  F1.2_matrix_fallback:    DONE
  F2.1_safety_levels:      DONE
  F2.2_crop_flip:          DONE
  F2.3_speed_trim:         DONE
  F2.4_selfcheck_fission:  DONE
  F3.1_rules_json:         DONE      # 2026-08-03 23:34 落地，67 passed（48 -> 67）
  F3.2_server_params:      DONE      # 2026-08-04 落地，75 passed（67 -> 75），GitHub b66f372
  F3.3_hook_whitelist:     DONE      # 2026-08-04 落地，83 passed（75 -> 83），GitHub ee8634f
  F4.1_web_controls:       DONE
  F4.2_phash_render:       DONE
  F5.1_skill_sop:          DONE      # 2026-08-04，Skills SOP + 诊断闭环 + README 索引
  DD_closeout_regression:  DONE      # 默认5契约统一 + Web手测清单 + real-ffmpeg e2e
  runtime_contract_closeout: DONE    # requestState绑定 + Pre Hook fail-closed + 本地来源门 + 交付门
  Q03_objective_av_sync:   DONE      # 31s节拍音轨 heavy 1.10x；音视频流时长差<=0.10s

# ---------------- 进度（05-扩展功能 · 配音链）----------------
progress_extension:
  F-配音-001_tts_client:      DONE            # MiMo v2.5，MIMO_API_KEY 从 .env 自动加载
  F-配音-002_audio_replace:   DONE            # dedup 产物换轨；apad 补静音 + -shortest
  F-配音-003_subtitle_text:   DONE            # ffmpeg -map 0:s:0 提字幕
  F-配音-005_asr:             DONE            # sherpa-onnx Paraformer 本地识别
  F-配音-006_deepseek_rewrite: DEPRECATED        # 已停用；全迁移至元宝
  F-配音-007_vision_yuanbao:   DONE                 # 元宝识图 + 改写；服务器端仅提取上下文用，改写委托给浏览器扩展
  F-配音-008_server_headless_login: CLOSED_UNRESOLVED  # 服务器无头 Chromium 扫码登录元宝：cookie 无 token → 死胡同
  F-配音-009_browser_extension: IN_PROGRESS          # 浏览器扩展元宝改写桥接；manifest + bg + content 已创建，待端到端验证

# ---------------- 下一步（仅 QA 可收口）----------------
next_action:
  - id: independent_qa_signoff
    what: "实现侧已执行浏览器步骤 5-7 并形成 docs/eval/F5.1-浏览器步骤5-7执行报告.md（执行结果 PASS）；独立 QA 成员须按 station/web/README-手测清单.md 复核或重跑并签字"
    acceptance: "确认控件默认值与联动、五项自检展示（实现侧本轮为 signature 兜底）、裂变矩阵、delete_output 第4级硬阻断、审计日志落盘；失败只记录为失败，不得视为通过或实现补充"
    blocker: "2026-08-04 再次启动独立 QA 执行者返回平台 403，未发生测试执行，签字保持 PENDING"
  - id: handoff_review
    what: "仅收到独立 QA 签字后，才能把 lifecycle 改为 SUCCESS"
  - id: deepseek_login_selfproof
    what: "用户已通过前端Web登录+改写预览完成端到端验证；playwright locator.fill+keyboard.press 解决 React isTrusted 问题"
    status: DONE 2026-08-11
  # fix_4_failing_tests: DONE 2026-08-10（166 passed），已移出待办

# ---------------- 本阶段关键结论（勿重复踩）----------------
locked_conclusions:
  metric_caliber: "达标 = phash_avg>=12 且 weak_frame_ratio<=0.10；phash_min 已退出判定（仅展示）"
  separation_legs: >
    变体间只有两条有效腿：时间错位（δ>=1s）与 flip。
    speed 在归一化抽帧口径下【恒等】(avg 1.875)，rotate 4.12 / crop 7.50 都够不到 12。
    -> 不要再试「反向 speed / 铺开 crop」拉开变体，均已实测否证。
  fission_fix: "batch_fission 默认 5、上限 20；按 phase=i/(count-1) 确定性铺开 trim 头尾配比；最坏对 3.5 -> 18.5"
  q03_av_sync: "31s 合成节拍音轨 heavy 1.10x 后 duration_close=true，音频/视频流时长差<=0.10s；无需长视频限档分支"
  short_asset_boundary: >
    <7s 素材 trim 被最短时长保护挡掉 -> 时间腿不存在，变体间仅 0.5-1.75。
    唯一杠杆是各变体不同 flip_mode（实测 28.75-35.88）。flip 默认关是 PRD 人工决策点，
    故未自动开，改为 batch_fission 返回 separation{time_leg,flip_spread,hint} 指明卡哪条腿。
  browser_profile_lock: >
    浏览器自动化【禁止】借用用户日常 Edge/Chrome 的 User Data 目录：Chromium 单例锁
    会让 launch_persistent_context 抛 TargetClosedError，表现为页面停在 about:blank。
    一律用专用 profile（station/logs/.deepseek-profile，VU_DEEPSEEK_PROFILE 可覆盖）
    + 一次性 --login。用专用目录后改写与日常浏览可并行，无需关浏览器。
  deepseek_login_detection: >
    判 DeepSeek 登录态【不要】读 cookie：匿名访问 /sign_in 即下发 HWWAFSESID/
    HWWAFSESTIME/ds_session_id，复访又多 smidV2 与 .thumbcache_*，靠名单区分 = 长期猜谜。
    唯一可靠信号 = 打开页面后是否被重定向到 /sign_in。
  headless_ua: "headless 自报 HeadlessChrome，DeepSeek 前置华为 WAF 会当爬虫；须读真实 UA 去掉 Headless 标记，勿硬编码版本号"
  tts_text_hygiene: "tts_client.tts() 不做任何文本清洗；网页回复的换行/markdown 星号/「文案：」前缀必须在 copy_rewriter._clean_reply 里清干净，否则会被朗读出来"
  detail_ref: "数值全表 -> docs/eval/验证结果.md；归因与错题本 -> docs/eval/沉淀失败原因.md"
  server_headless_yuanbao_login_dead: >
    2026-08-11 完整 E2E 确认：服务器 Linux headless Chromium 下元宝 QR 扫码登录，
    cookies 文件正常落盘（20480 字节），但内容仅有匿名指纹（_qimei/_ga/_TDID_CK），
    **不含登录 token**。新 Chromium session 读 cookies 后页面仍显示"未登录"。
    原因：元宝 QR 登录的 iframe（open.weixin.qq.com）redirect 链在 headless 模式下
    未正确写入 auth cookie/可能用了 localStorage。`--disable-encryption-cookies` 已生效，
    确认非加密问题。**服务器端无头元宝登录不可行**，此为终局结论，勿重复尝试。
    唯一零 token 可行路径：用户在本地 GUI 浏览器跑 yuanbao_local_proxy.py 做改写。

# ---------------- 红线（勿违背）----------------
constraints:
  stack: "Python 标准库 + 原生 JS + ffmpeg；本地单用户；无数据库/无登录/无 Cookie。禁引入 Hono/Prisma/Better Auth/SQL"
  mcp_contract: "input_required + requestState 不变；inputResponses.confirm=true 放 params 顶层；requestState 必须绑定原 name+arguments；普通成功返回 {content,isError}"
  hooks_evaluator: "不改 hooks/common.py tier 求值器（只读顶层字段）；PreToolUse 故障必须 fail-closed"
  delivery_gate: "单条 checks.all_passed=true 才可确认交付；裂变 delivery_ready=all_unique && matrix.all_pass"
  ffmpeg: "一律 subprocess 列表式传参，禁 shell 拼接"
  frontend: "一律 escapeHtml/textContent"
  delete_output: "保持 blocked 硬阻断"
  thresholds: "metrics 阈值/LEVELS 常量本阶段不再调整（口径已定案）"
  hygiene: "每次改完跑 pytest；临时产物用完即删（微笑_*、下班来接我_*、剑鬼_变体* 是历史文件，保留）"

# ---------------- 素材 ----------------
assets:
  station/assets/: ["微笑.mp4 (5.06s 896x992, <7s 短素材用例)", "下班来接我.mp4 (49.48s 1070x1914)"]
  station/assets/test/: ["剑鬼.mp4 (8.0s 720x1280, F2.4 复现用例)", "龙.mp4 (15.2s)"]
```
