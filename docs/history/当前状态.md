# 当前状态（项目快照）

> **维护守则**（承 docs/INDEX.md §1）
> 本文件是**唯一的"现在走到哪了"快照**，切片 1 视角，**每跨阶段整块重写**（不做增量追加，历史流水在 `开发过程.md`）。
> 只写"接手就能干活"所需：位置 / 启动 / 进度 / 下一步 / 红线。数值实验与失败归因不进此文件，指向 `docs/eval/`。

```yaml
snapshot: 2026-08-14T08:56:00+08:00
project: "视频去重 Agent 工位（video-uniqueness）"
phase: "P3 桌面端本地后端 + 系统 Edge CDP 驱动元宝 — 改写提示词发送阻断待解"
lifecycle: ACTIVE

# ---------------- 架构概览 ----------------
architecture:
  frontend: "Chrome/Edge MV3 浏览器扩展（station/extension/，v1.0 验收）"
  desktop: "Electron 桌面壳（station/desktop/，2026-08-12 新增，与扩展并存；走元宝走 BrowserWindow）"
  gateway: "background.js Service Worker → MCP JSON-RPC over HTTP"
  desktop_yuanbao: "改用系统 Edge 浏览器 CDP 驱动（station/server/yuanbao_client.py 经 --remote-debugging-port 动态端口连接，复用已登录元宝；2026-08-13 起替代 Electron 内置 BrowserWindow + content-yuanbao.js，用户明确要复用其系统浏览器登录态）"
  server: "本地 venv: station/server/mcp_server.py 由 station/desktop/lib/local-server.js 拉起，监听 127.0.0.1:8765（桌面端专用）；远端 124.71.209.36:8765 为旧 web，已弃用、桌面端禁止依赖"
  asr: "sherpa-onnx paraformer 在 F:/Download/A-models/sherpa-onnx（VU_ASR_MODELS 覆盖；extract_copy_context 默认 /opt 路径不适用本机）"
  tts: "MiMo (mimo-v2.5-tts) via OpenAI-compatible API, key=sk-cbpqn..."
  web_legacy: "archive/web/（被扩展与桌面端共享 loadFile 复用）"

# ---------------- 位置与启动 ----------------
location: "F:/ai_agent/dev2/xiaomi/ai/test11/video-uniqueness/"
layout:
  station/extension/: "浏览器扩展（popup.html/js + background.js + content-yuanbao.js + style.css）"
  station/desktop/: "Electron 桌面壳（main.js + preload.js + lib/* + package.json）"
  station/desktop/lib/: "window/menu/download/api-base/logger + yuanbao-window.js + preload-yuanbao.js"
  station/server/: "MCP 服务器（mcp_server.py + pipeline.py + tts_client.py + asr_client.py + metrics.py）"
  archive/web/: "旧 Web 前端（被扩展 popup 与桌面端 loadFile 共享复用）"
  docs/05-扩展功能/changes/add-desktop-electron/: "桌面端变更 OpenSpec 四件套"
  docs/: "项目文档（INDEX.md → history/ + eval/ 双轨制）"
run:
  server_start: "SSH to 124.71.209.36 → systemctl start video-dedup"
  server_status: "systemctl is-active video-dedup → active (running)"
  server_deploy: "paramiko SFTP upload station/server/*.py → systemctl restart video-dedup"
  extension_refresh: "edge://extensions → 刷新扩展"
  desktop_start: "cd station/desktop && npm install（首次）&& npm start"
  desktop_devtools: "Ctrl+Shift+I 在桌面端启用 DevTools"
  test: "C:/Users/lucky/.workbuddy/binaries/python/envs/default/Scripts/python.exe -m pytest station/tests -q"

# ---------------- 进度 ----------------
progress:
  F1.1_pair_metric:        DONE
  F1.2_matrix_fallback:    DONE
  F2.1_safety_levels:      DONE
  F2.2_crop_flip:          DONE
  F2.3_speed_trim:         DONE
  F2.4_selfcheck_fission:  DONE
  F3.1_rules_json:         DONE
  F3.2_server_params:      DONE
  F3.3_hook_whitelist:     DONE
  F4.1_web_controls:       ARCHIVED  # 旧 Web → archive/web/，扩展替代
  F4.2_phash_render:       DONE
  F5.1_skill_sop:          DONE
  X1_extension_skeleton:   DONE  # 2026-08-09 MV3 扩展骨架
  X2_rewrite_flow:         DONE  # 2026-08-10/11 元宝改写全链路（content-yuanbao.js）
  X3_tts_integration:      DONE  # 2026-08-11 MiMo TTS 配音
  X4_download_route:       DONE  # 2026-08-12 /local/download 路由实现
  X5_skip_phash:           DONE  # 2026-08-12 跳过 pHash 自检（3-5min → 1-2min）
  X6_output_recovery:      DONE  # 2026-08-12 list_outputs + 查看最近产物
  D1_desktop_skeleton:    DONE  # 2026-08-12 station/desktop/ + lib/* + preload + main
  D2_desktop_load_web:    DONE  # 2026-08-12 loadFile archive/web + CSP meta + API_BASE 兜底
  D3_desktop_download:    DONE  # 2026-08-12 will-download + 保存对话框 + 进度回传
  D4_desktop_ipc_bridge:  DONE  # 2026-08-12 desktop.onDownloadProgress + openExternal + runYuanbaoRewrite + onYuanbaoDone
  D5_desktop_packaging:   PLANNED  # 占位 F-桌面-005，本轮不实现
  D6_desktop_yuanbao_view: REWORKED  # 2026-08-13 弃用 Electron 内置 BrowserWindow，改系统 Edge CDP 驱动（见 P3 / edge_cdp_yuanbao_config）
  D7_local_backend:        DONE  # 2026-08-13 lib/local-server.js 拉起本地 venv mcp_server.py @127.0.0.1:8765
  D8_edge_cdp_yuanbao:     PARTIAL  # 2026-08-14 系统 Edge 经 CDP 跳转元宝成功；提示词(文案+截图)发送未通，待修

# ---------------- 下一步 ----------------
next_action:
  - id: fix_yuanbao_send
    what: "Edge CDP 已能跳转元宝，但文案+截图的提示词未被发送（输入框定位 / 图片上传 / file_chooser 任一处失败）。排查 station/server/yuanbao_client.py REWRITE_TEMPLATE 的输入框选择器与图片上传流程，参考 station/extension/content-yuanbao.js 的 DOM driver"
  - id: verify_edge_login_persist
    what: "确认复制的登录态在持久调试 profile 能被 ABE 解密；若调试窗口弹登录框，改用户在该窗口扫码一次（profile 持久，后续免登录）"
  - id: verify_desktop_e2e
    what: "桌面端 1.6 全链路验收（上传→探测→本地 extract_copy_context→Edge CDP 改写→TTS→原生下载）"
  - id: archive_change
    what: "1.6 通过后归档 changes/add-desktop-electron/ → archive/，合并 delta 进 DD"
  - id: future
    what: "electron-builder 打包（F-桌面-005）；OSS 迁移"

# ---------------- 本阶段关键教训（勿重复踩）----------------
locked_conclusions:
  tier_override: >
    dedup_video._tier 在多次 SFTP 部署中被从 "audit" 覆盖回 "warned"。
    根因：每次单独上传 pipeline.py 或 mcp_server.py 而非两文件一起。
    对策：始终同时上传两文件；部署后立即 curl MCP tools/list 验证 _tier。
  download_route_missing: >
    /local/download/<filename> 路由从 v1.0 起就未实现，do_GET/_serve_web 只服务 _WEB_FILES。
    产物统统生成在服务器但下载从不工作 → 用户反复认为"没有产物"。
  phash_bottleneck: >
    M.compare_videos 在 2 核 ECS 上跑 3-5 分钟 100% CPU。default skip_phash=true 可绕过。
    如果需要自检，应异步化（先返回产物链接，后补 pHash 结果）。
  env_var_drift: >
    VU_HOST/VU_FFMPEG/VU_FFPROBE/VU_ALLOWED_HOSTS/MIMO_API_KEY 均在 deploy 中流失过。
    systemd Environment= 写全所有变量，不再依赖 .env 文件。
  tts_key_lifecycle: >
    MiMo API Key 换过 3 次。key1 无效→key1 过期挂起→key2 有效。
    TTS 401 暴露在 UI 让用户知道可降级。
  storage_clear_loses_link: >
    dedupResult 只在服务端返回后写入 chrome.storage.local。
    用户若在 pHash 自检期间清 storage/reload → 链接永久丢失。
    补救：btn-recover-outputs（list_outputs MCP）+ /local/download 路由。
  yuanbao_server_side_unreliable: >
    部署的 MCP yuanbao_login / rewrite_copy 走服务端 headless Chromium 自动化（DeepSeek 后端），
    按 2026-08-11 决策已冻结；当前不可用：yuanbao_login 返回无 QR 数据，
    rewrite_copy 报"DeepSeek 未登录"。
    对策经两轮演进：(1) 2026-08-12 改用 Electron 浮动 BrowserWindow + content-yuanbao.js 在客户端做 DOM 自动化；
    (2) 2026-08-13 用户明确要求复用其系统 Edge 登录态，故彻底改为 station/server/yuanbao_client.py
    经 CDP 驱动系统 Edge（--remote-debugging-port 动态端口 + 持久调试 profile 复制登录态），
    桌面端不再用内置 BrowserWindow，也不调 MCP rewrite_copy。
    元宝登录按钮仍走 shell.openExternal('https://yuanbao.tencent.com/') 跳系统浏览器。
  server_tool_drift: >
    本地源码含 extract_copy_context，部署的服务器不含；本地无 yuanbao_login/rewrite_copy，部署版含。
    本地源码与部署可能存在版本漂移。改前端前必须 curl 124.71.209.36:8765/mcp tools/list 确认实际工具集。
  desktop_archive_web_sharing: >
    station/desktop 不复制 archive/web/，disk 共享 loadFile 加载；
    任何 archive/web/app.js 改动自动双端（扩展 popup + 桌面端）生效。
    Bugfix 优先在 archive/web 改一处即可。
  local_backend_mandatory: >
    桌面端 MCP 后端必须本地拉起（lib/local-server.js 自动 spawn venv mcp_server.py @127.0.0.1:8765），
    禁止依赖远端 124.71.209.36（旧 web，已弃用）。extract_copy_context 等工具只在本机后端可用。
  edge151_no_debug_on_default_profile: >
    Edge 151 二进制明确禁止在默认 profile 开调试端口（"DevTools remote debugging requires a non-default data directory"，
    扫描二进制字符串确认）。必须用独立 profile（station/server/logs/.edge-debug-profile）。
  edge_cookies_locked_while_running: >
    Edge 运行时 Cookies 库被文件级独占锁死（open/copy2/sqlite ro/esentutl 全部读不到，仅 Local State 可读）。
    复制登录态前必须 taskkill 关一次 Edge 释放锁；这是唯一躲不掉的一步。
  never_hardcode_debug_port: >
    调试端口禁止写死 9223：残留进程占用会让新 Edge 静默绑定失败→轮询超时。改用 _free_port() 动态选空闲端口。
  keep_users_edge: >
    复制登录态后必须把用户原 Edge 重新拉起（恢复标签页）；改写结束仅按 PID 树回收临时调试实例（taskkill /PID /T），
    绝不误杀用户浏览器；用户手动关调试实例也不影响原浏览器。
  subprocess_stdout_must_be_fileobj: >
    subprocess.Popen(stdout=...) 绝不能传字符串路径（会当文件对象调 .fileno() 抛 'str' object has no attribute 'fileno'，
    导致调试 Edge 从未启动）。必须 open() 真正的文件对象传入。

# ---------------- 红线（勿违背）----------------
constraints:
  stack: "Python stdlib + Vanilla JS + ffmpeg; local single-user; no DB/login/Cookie"
  mcp_contract: "input_required + requestState; inputResponses.confirm=true（确认流由 callToolWithConfirm 自动处理）"
  hooks: "tier flat-field evaluator unchanged; PreToolUse fail-closed"
  delivery: "checks.all_passed=true 才交付"
  ffmpeg: "subprocess list argv, no shell concat"
  frontend: "escapeHtml/textContent always; 不在 archive/web/ 造新 UI（与 web 项目原版一致）"
  delete_output: "BLOCKED"
  thresholds: "frozen, per DD"
  deploy: "SFTP upload pipeline.py + mcp_server.py TOGETHER; verify _tier post-deploy"
  desktop_security: "contextIsolation:true + nodeIntegration:false + sandbox:true + CSP meta 限定 connect-src"
  yuanbao_login_button: "openExternal 跳系统浏览器，不调 MCP，不弹 confirm modal"
  yuanbao_rewrite: "桌面端走系统 Edge CDP（yuanbao_client.py 经动态端口连接，复用已登录元宝）；不调 MCP rewrite_copy；不用 Electron 内置 BrowserWindow"
  desktop_backend: "本地 lib/local-server.js 拉起 venv mcp_server.py @127.0.0.1:8765；禁止依赖远端 124.71.209.36"

# ---------------- 服务端环境（远端 124.71.209.36，历史参考；桌面端 P3 已改本地 127.0.0.1:8765，禁止依赖远端）----------------
server_env:
  host: "124.71.209.36 (2-core ECS, Debian 12) — 旧 web，桌面端已不依赖"
  service: "video-dedup.service (active, port 8765)"
  venv: "/opt/video-dedup/.venv/bin/python"
  env_vars:
    VU_HOST: "0.0.0.0"
    VU_FFMPEG: "/usr/bin/ffmpeg"
    VU_FFPROBE: "/usr/bin/ffprobe"
    VU_ALLOWED_HOSTS: "vu.evenblue.top,124.71.209.36,localhost,127.0.0.1"
    MIMO_API_KEY: "sk-cbpqn66amnedyg6sbvqgk24fz8aipdycbekupl44rsra62to"
  asr: "/opt/sherpa-onnx (paraformer, is_available=True)"
  output_dir: "/opt/video-dedup/output/ (current: 7 files, ~650MB total)"
  input_dir: "/opt/video-dedup/input/ (auto-clean >1h)"
  deployed_mcp_tools: "[list_assets, list_outputs, probe_video, dedup_video, batch_fission, list_watermark_templates, remove_watermark, yuanbao_login, rewrite_copy, get_job, delete_output]"

# ---------------- 桌面端元宝 Edge CDP 配置（P3，2026-08-13 起）----------------
edge_cdp_yuanbao_config:
  driver: "station/server/yuanbao_client.py（独立子进程跑 Playwright，connect_over_cdp 接系统 Edge）"
  edge_exe: "C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe（自动探测 x64 路径）"
  debug_profile: "station/server/logs/.edge-debug-profile（持久；首次关闭用户 Edge 复制登录态播种一次，之后复用）"
  launch_args: "--remote-debugging-port=<动态空闲端口> --user-data-dir=<debug_profile> --new-window https://yuanbao.tencent.com/"
  state_file: "station/server/logs/edge_debug_state.json（记录 port/pid/profile，供下次复用/自愈）"
  diag_log: "station/server/logs/yuanbao_debug.log（改写全程日志，本地排查首查此文件）"
  flow:
    - "桌面端 app.js：btn-rewrite-preview → extract_copy_context 取 raw_text + frames_b64 + max_chars"
    - "lib/yuanbao-window.js：run-rewrite IPC → 拉起 venv python yuanbao_client.py --rewrite"
    - "ensure_edge_debug_port()：复用已有实例 / 关 Edge 复制登录态 / 启动调试 Edge / 等 CDP 就绪"
    - "REWRITE_TEMPLATE：CDP 连接 → 选元宝页 →（检测登录弹窗）→ 上传图片 + 填提示词 + 回车 → 读回复"
  fallback: "若复制的登录态被 ABE 拦住，调试窗口弹登录框，用户在窗口内扫码一次即可（profile 持久，后续免登录）"
  shortcuts: "无独立快捷键；调试 Edge 为独立窗口"

# ---------------- 素材 ----------------
assets:
  input/: ["2.mp4 (103MB, uploaded 8/12)"]
  output/: ["2_去重_6.mp4 (last, 104MB, 16:08)", "2_去重.mp4 through 2_去重_6.mp4"]