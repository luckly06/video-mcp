# 当前状态（项目快照）

> **维护守则**（承 docs/INDEX.md §1）
> 本文件是**唯一的"现在走到哪了"快照**，切片 1 视角，**每跨阶段整块重写**（不做增量追加，历史流水在 `开发过程.md`）。
> 只写"接手就能干活"所需：位置 / 启动 / 进度 / 下一步 / 红线。数值实验与失败归因不进此文件，指向 `docs/eval/`。

```yaml
snapshot: 2026-08-12T16:29:53+08:00
project: "视频去重 Agent 工位（video-uniqueness）"
phase: "P1 浏览器扩展全链路 — 功能完整，稳定性收口"
lifecycle: ACTIVE

# ---------------- 架构概览 ----------------
architecture:
  frontend: "Chrome/Edge MV3 浏览器扩展（station/extension/）"
  gateway: "background.js Service Worker → MCP JSON-RPC over HTTP"
  server: "124.71.209.36:8765（Debian 12, systemd video-dedup.service）"
  asr: "sherpa-onnx paraformer at /opt/sherpa-onnx（is_available=true）"
  tts: "MiMo (mimo-v2.5-tts) via OpenAI-compatible API, key=sk-cbpqn..."
  web_legacy: "archive/web/（已归档；扩展是唯一前端）"

# ---------------- 位置与启动 ----------------
location: "F:/ai_agent/dev2/xiaomi/ai/test11/video-uniqueness/"
layout:
  station/extension/: "浏览器扩展（popup.html/js + background.js + content-yuanbao.js + style.css）"
  station/server/: "MCP 服务器（mcp_server.py + pipeline.py + tts_client.py + asr_client.py + metrics.py）"
  archive/web/: "旧 Web 前端（已归档，只读参考）"
  docs/: "项目文档（INDEX.md → history/ + eval/ 双轨制）"
run:
  server_start: "SSH to 124.71.209.36 → systemctl start video-dedup"
  server_status: "systemctl is-active video-dedup → active (running)"
  server_deploy: "paramiko SFTP upload station/server/*.py → systemctl restart video-dedup"
  extension_refresh: "edge://extensions → 刷新扩展"
  test: "C:/Users/lucky/.workbuddy/binaries/python/envs/default/Scripts/python.exe -m pytest station/tests -q"

# ---------------- 进度 ----------------
progress:
  F1.1_pair_metric:        DONE
  F1.2_matrix_fallback:    DONE
  F2.1_safety_levels:      DONE
  F2.2_crop_flip:          DONE
  F2.3_speed_trim:         DONE
  F2.4_selfcheck_fission:  DONE
  F3.1_rules_json:         DONE
  F3.2_server_params:      DONE
  F3.3_hook_whitelist:     DONE
  F4.1_web_controls:       ARCHIVED  # 旧 Web → archive/web/，扩展替代
  F4.2_phash_render:       DONE
  F5.1_skill_sop:          DONE
  X1_extension_skeleton:   DONE  # 2026-08-09 MV3 扩展骨架
  X2_rewrite_flow:         DONE  # 2026-08-10/11 元宝改写全链路
  X3_tts_integration:      DONE  # 2026-08-11 MiMo TTS 配音
  X4_download_route:       DONE  # 2026-08-12 /local/download 路由实现
  X5_skip_phash:           DONE  # 2026-08-12 跳过 pHash 自检（3-5min → 1-2min）
  X6_output_recovery:      DONE  # 2026-08-12 list_outputs + 查看最近产物

# ---------------- 下一步 ----------------
next_action:
  - id: stablize
    what: "验证 skip_phash 实际耗时（预期 ffmpeg alone 1-2min），确认下载流畅"
  - id: document
    what: "更新 docs/eval/ 沉淀本轮发现的关键教训（tier 覆盖、下载路由缺失、pHash 瓶颈）"
  - id: future
    what: "pHash 异步化（后台自检不阻塞返回）、批量裂变 stability、平台真实判重回收"

# ---------------- 本阶段关键教训（勿重复踩）----------------
locked_conclusions:
  tier_override: >
    dedup_video._tier 在多次 SFTP 部署中被从 "audit" 覆盖回 "warned"。
    根因：每次单独上传 pipeline.py 或 mcp_server.py 而非两文件一起。
    对策：始终同时上传两文件；部署后立即 curl MCP tools/list 验证 _tier。
  download_route_missing: >
    /local/download/<filename> 路由从 v1.0 起就未实现，do_GET/_serve_web 只服务 _WEB_FILES。
    产物统统生成在服务器但下载从不工作 → 用户反复认为"没有产物"。
  phash_bottleneck: >
    M.compare_videos 在 2 核 ECS 上跑 3-5 分钟 100% CPU。default skip_phash=true 可绕过。
    如果需要自检，应异步化（先返回产物链接，后补 pHash 结果）。
  env_var_drift: >
    VU_HOST/VU_FFMPEG/VU_FFPROBE/VU_ALLOWED_HOSTS/MIMO_API_KEY 均在 deploy 中流失过。
    systemd Environment= 写全所有变量，不再依赖 .env 文件。
  tts_key_lifecycle: >
    MiMo API Key 换过 3 次。key1 无效→key1 过期挂起→key2 有效。
    TTS 401 暴露在 UI 让用户知道可降级。
  storage_clear_loses_link: >
    dedupResult 只在服务端返回后写入 chrome.storage.local。
    用户若在 pHash 自检期间清 storage/reload → 链接永久丢失。
    补救：btn-recover-outputs（list_outputs MCP）+ /local/download 路由。

# ---------------- 红线（勿违背）----------------
constraints:
  stack: "Python stdlib + Vanilla JS + ffmpeg; local single-user; no DB/login/Cookie"
  mcp_contract: "input_required + requestState; inputResponses.confirm=true"
  hooks: "tier flat-field evaluator unchanged; PreToolUse fail-closed"
  delivery: "checks.all_passed=true 才交付"
  ffmpeg: "subprocess list argv, no shell concat"
  frontend: "escapeHtml/textContent always"
  delete_output: "BLOCKED"
  thresholds: "frozen, per DD"
  deploy: "SFTP upload pipeline.py + mcp_server.py TOGETHER; verify _tier post-deploy"

# ---------------- 服务端环境 ----------------
server_env:
  host: "124.71.209.36 (2-core ECS, Debian 12)"
  service: "video-dedup.service (active, port 8765)"
  venv: "/opt/video-dedup/.venv/bin/python"
  env_vars:
    VU_HOST: "0.0.0.0"
    VU_FFMPEG: "/usr/bin/ffmpeg"
    VU_FFPROBE: "/usr/bin/ffprobe"
    VU_ALLOWED_HOSTS: "vu.evenblue.top,124.71.209.36,localhost,127.0.0.1"
    MIMO_API_KEY: "sk-cbpqn66amnedyg6sbvqgk24fz8aipdycbekupl44rsra62to"
  asr: "/opt/sherpa-onnx (paraformer, is_available=True)"
  output_dir: "/opt/video-dedup/output/ (current: 7 files, ~650MB total)"
  input_dir: "/opt/video-dedup/input/ (auto-clean >1h)"

# ---------------- 素材 ----------------
assets:
  input/: ["2.mp4 (103MB, uploaded 8/12)"]
  output/: ["2_去重_6.mp4 (last, 104MB, 16:08)", "2_去重.mp4 through 2_去重_6.mp4"]
```
