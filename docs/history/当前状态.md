# 当前状态（项目快照）

> **维护守则**（承 docs/INDEX.md §1）
> 本文件是**唯一的“现在走到哪了”快照**，跨阶段整块重写；历史流水只放 `开发过程.md`。
> 只写接手即可继续工作的事实：当前主线 / 启动方式 / 已完成 / 下一步 / 红线。数值验证与失败归因指向 `docs/eval/`。

```yaml
snapshot: 2026-08-19
project: "视频去重 Agent 工位（video-uniqueness / video-mcp）"
lifecycle: ACTIVE
phase: "P4 桌面端发布前收口：发布态 MiMo TTS 配置修复"

# ---------------- 当前主线标识（必须保留）----------------
current_line:
  product: "Electron 桌面端（station/desktop/）"
  server: "本机 Python MCP 后端（station/server/mcp_server.py），由 station/desktop/lib/local-server.js 在 npm start 时自动拉起"
  ui_source: "archive/web/ 当前仍被桌面端 loadFile 复用，是桌面端 UI 源；不要按旧 web 站点理解"
  yuanbao: "系统 Edge CDP 驱动（station/server/yuanbao_client.py + station/desktop/lib/yuanbao-window.js），使用桌面端 userData 下的独立调试 Edge profile；首次扫码一次后复用登录态；禁止关闭用户正在使用的系统 Edge"
  tts: "MiMo mimo-v2.5-tts；Python 标准库直连，不依赖 openai；发布态从 app.asar 内加密运行配置注入 MIMO_API_KEY；默认关闭，只有单条去重且用户显式勾选时启用"
  package: "Windows portable exe：station/desktop/dist/视频去重工位-0.1.0-x64.exe（图标来自用户指定 1.png）"
  upload_assets: "打包态上传素材目录：%USERPROFILE%/Videos/视频去重素材（由 VU_ASSETS 注入 Python VIDEO_DIR）"
  legacy_notice: "后续文档中凡涉及 web 端、Chrome/Edge 扩展插件、远端 124.71.209.36 的描述，默认均为旧版/历史参考；当前交付不再依赖远端 web 或扩展插件"

# ---------------- 架构概览 ----------------
architecture:
  desktop: "station/desktop/: Electron 壳 + preload IPC + 下载/目录选择/本地后端拉起"
  local_backend: "station/server/: mcp_server.py / pipeline.py / asr_client.py / tts_client.py / yuanbao_client.py"
  frontend: "archive/web/: 桌面端当前加载的主 UI；扩展与旧 web 相关描述均视为历史"
  output_config: "~/.video-uniqueness/config.json 保存 dedup_output_dir / fission_output_dir"
  output_layout: "去重/裂变分别使用用户配置根目录，并按 {原视频名}__{源视频MD5前8位}/ 建素材工作区"
  asr_models: "F:/Download/A-models/sherpa-onnx（通过 VU_ASR_MODELS 覆盖 Linux 默认路径）"

# ---------------- 位置与启动 ----------------
location: "F:/ai_agent/dev2/xiaomi/ai/test11/video-uniqueness/"
run:
  desktop_start: "PowerShell: cd F:\\ai_agent\\dev2\\xiaomi\\ai\\test11\\video-uniqueness\\station\\desktop; npm start"
  desktop_build: "PowerShell: cd F:\\ai_agent\\dev2\\xiaomi\\ai\\test11\\video-uniqueness\\station\\desktop; npx electron-builder --dir --win --x64 --publish never"
  desktop_dist: "F:/ai_agent/dev2/xiaomi/ai/test11/video-uniqueness/station/desktop/dist/win-unpacked/视频去重工位.exe（目录版；分发请压缩整个 win-unpacked 文件夹）"
  desktop_assets: "%USERPROFILE%/Videos/视频去重素材"
  backend_auto: "npm start 自动 spawn station/server/mcp_server.py，监听 127.0.0.1:8765"
  desktop_devtools: "Ctrl+Shift+I"
  syntax_check:
    js: "node --check archive/web/app.js"
    py: "python -m py_compile station/server/mcp_server.py station/server/pipeline.py"
  remote: "origin=https://github.com/luckly06/video-mcp.git；用户未明确要求 push 时不得推送"

# ---------------- 已完成进度 ----------------
progress:
  D7_local_backend: DONE
  D8_edge_cdp_yuanbao: DONE
  D9_rewrite_to_tts: DONE
  D10_tts_fail_popup: DONE
  D11_orb_loader_fix: DONE
  D12_rewrite_race_fix: DONE
  D13_tts_meta_clean: DONE
  D14_duration_label_dyn: DONE
  D15_tts_dur_probe_fix: DONE
  D16_tts_mode_rainbow: DONE
  D17_duration_neutral: DONE
  D18_sidebar_sections_collapse: DONE
  D19_result_download_button: DONE
  D20_top_download_reverted: DONE
  D21_right_sidebar_collapse: DONE
  D22_batch_fission_tts_note: DONE
  D23_split_output_dirs: DONE
  D24_output_directory_card: DONE
  D25_config_dedup_fission_dirs: DONE
  D26_top_sidebars_toggle: DONE
  D27_sidebar_state_overlap_fix: DONE
  D28_output_config_badge: DONE
  D29_asset_workspace_output: DONE
  D30_desktop_font_scale: DONE
  D31_dimension_faq: DONE
  D32_desktop_exe_package: DONE
  D33_desktop_packaged_upload_assets: DONE
  D34_yuanbao_frame_upload_fix: DONE
  D35_tts_warning_scope: DONE
  D36_yuanbao_debug_login_profile: DONE
  D37_desktop_local_hardening: DONE
  D38_yuanbao_rewrite_isolation_no_edge_kill: DONE
  D39_tts_enable_explicit_gate: DONE
  D40_packaged_tts_runtime_config: DONE
  D41_open_current_artifact_folder: DONE
  D42_packaged_mcp_port_fallback: DONE
  D43_release_secret_sanitization: DONE

# ---------------- 本轮关键实现 ----------------
current_changes:
  output_dirs:
    - "前端【产物目录】独立卡片：分别显示/打开/配置去重产物文件夹与裂变产物文件夹"
    - "首次未配置时卡片高亮，显示『待配置输出文件夹』；两个目录均配置后自动消失"
    - "开始单条去重 / 开始裂变前分别确认对应目录；用户取消则中止任务"
  workspace_layout:
    - "后端按素材生成工作区：{原视频名}__{源视频MD5前8位}"
    - "去重产物和裂变产物分别写入各自根目录下的素材工作区"
    - "前端展示普通文件名，内部保留相对路径用于下载/打开"
  download_safety:
    - "/local/download/<relative_path>?subdir=去重|裂变 支持素材工作区相对路径"
    - "后端 relative_to 校验确保文件必须位于对应产物根目录内"
  sidebar_ui:
    - "右侧栏支持单独收纳；顶部新增一键收纳/展开左右侧栏按钮"
    - "修复右侧单独收纳后再顶部收纳导致左侧空白的 CSS 状态叠加问题"
  font_scale:
    - "顶部栏新增【字号】控件：A− / 100%重置 / A＋，缩放范围 85%~125%，步进 5%"
    - "使用 localStorage(vu_ui_font_scale) 记忆用户字号偏好，页面重载后自动恢复"
    - "仅改 archive/web 桌面端 UI 源，不触碰本地后端、产物目录和视频处理链路"
  dimension_faq:
    - "【处理维度】正下方新增 FAQ 入口；解答内容默认隐藏，鼠标悬浮/键盘聚焦后弹出"
    - "弹出卡片面向小白解释画面、微旋转、裁切、变速、去头尾、翻转，并明确翻转默认关闭的原因"
  desktop_package:
    - "旧 dist 产物已删除后重新生成；当前 exe 为指定图标的新包"
    - "electron-builder 随包携带 archive/web、station/server、station/extension/content-yuanbao.js、station/vendor"
    - "开发态仍走仓库相对路径；打包态统一走 process.resourcesPath，避免 exe 内路径失效"
    - "轻量冒烟已确认 exe 进程可启动，完整视频端到端仍需用户本机验收"
  packaged_upload_fix:
    - "打包态本地后端启动时注入 VU_ASSETS=%USERPROFILE%/Videos/视频去重素材，并在写入前创建目录"
    - "打包态 preferFresh：若 8765 已有旧 MCP，自动尝试 8766-8774 拉起当前 exe 随包后端"
    - "修复上传时写入 AppData/Local/Temp 临时路径导致文件不存在的失败链路"
    - "用户验证新 exe 前应关闭旧 exe/旧 python 后端，避免旧进程继续占用端口或缓存前端"
  yuanbao_frame_upload_fix:
    - "copy_rewriter._extract_frames 的 ffmpeg duration 探测改为二进制 stderr + utf-8 errors=replace 解码，避免 Windows GBK UnicodeDecodeError 让 frames_b64 为空"
    - "mcp_server._build_copy_context 抽帧异常写 vu warning；yuanbao-window 记录 frames_b64/frame_files 数量，不输出图片内容"
    - "验证已覆盖 py_compile、node --check、ffmpeg 临时 mp4 抽帧回归（n=3 返回 2 帧）"
  tts_warning_scope:
    - "TTS 未生效原生弹窗仅保留在单条去重：用户启用手动文案或改写+配音，但产物 applied_params.tts_applied 未成功时提示"
    - "批量裂变当前明确不支持 TTS 配音，完成后不再弹 MIMO_API_KEY/openai 排查提示"
  yuanbao_debug_login_profile:
    - "【元宝登录】按钮改为调用桌面端 yuanbao:login-debug，不再用系统 Edge openExternal 打开独立登录页"
    - "yuanbao_client.py --login-debug 复用 AI 改写相同的 DEBUG_PROFILE 与 CDP 调试 Edge，扫码后 AI 改写预览直接复用该登录态"
    - "UI 提示改为『首次使用请在弹出的元宝调试窗口扫码』，降低小白用户登录到错误浏览器/Profile 的概率"
  desktop_local_hardening:
    - "发布包仅携带运行必需的 archive/web、station/server、station/vendor 资源，排除测试、调试、日志、.env 与 source map 等非交付文件"
    - "生产包隐藏开发者工具入口并屏蔽常见 DevTools 快捷键；开发态 npm run dev 保留调试能力"
    - "本轮为本地低风险加固，不引入服务器、不改变视频处理链路"
  yuanbao_rewrite_isolation_no_edge_kill:
    - "元宝改写 request_id 严格匹配，旧请求/旧视频的异步回包不再污染当前预览"
    - "元宝扫码与改写均走独立 userData debug profile；禁止把 profile 放在 package/dist 下，避免随包目录被替换或清理"
    - "禁止关闭用户正在使用的系统 Edge；桌面端只管理自己拉起的调试 Edge"
  tts_enable_explicit_gate:
    - "默认不启用 AI 配音；readTTS 先检查 chk-tts-enable，未勾选时不读取/不提交 TTS 文案"
    - "AI 改写预览仅预览文案；用户确认使用改写文案后，才自动勾选 AI 配音"
    - "批量裂变 doFission 不再 readTTS，也不向后端透传 TTS；mcp_server.py 仅 enable_tts=true 时消费 tts_text/rewrite_template"
    - "TTS 未生效提醒仅覆盖单条去重 + 用户显式勾选启用 AI 配音 + 配音未生效"
  packaged_tts_runtime_config:
    - "发布包继续排除明文 station/server/.env；beforePack 生成 AES-GCM runtime-config.bin 并装入 app.asar"
    - "local-server.js 启动随包后端时解密并通过环境变量注入 MIMO_API_KEY，不写入日志或磁盘明文配置"
    - "tts_client.py 使用 urllib 标准库调用 MiMo，用户环境无需 pip install openai"
    - "纯本地应用只能提高 Key 提取门槛，不能提供服务器侧零信任级保密"

  current_artifact_folder:
    - "【去重产物文件夹→打开】使用 currentDedupArtifact，打开本次任务工作区而非去重根目录"
    - "后端 open_parent 先做 relative_to 与文件存在校验，再打开当前产物父目录"
    - "无当前任务产物时提示先完成处理，不回退到可能混有旧文件的根目录"
    - "最终发布包排除 station/shared 与 station/hooks 下的 pyc/pyo/log 缓存文件，验包残留数为 0"
  packaged_mcp_port_fallback:
    - "8765 被旧进程占用但 MCP 探测失败时，新包不再停在失效端口"
    - "子进程就绪前退出会立即触发下一端口重试，候选范围为 8765-8774"
    - "页面 API_BASE 使用实际启动成功的端口，例如 http://127.0.0.1:8766"
  release_secret_sanitization:
    - "当前源码文档不再保存 MiMo API Key 明文"
    - ".env、runtime-config.bin、dist、output 与运行日志继续由 Git 忽略"
    - "历史提交中曾出现的 Key 必须在 MiMo 控制台轮换，单纯删除当前文本不能清除 Git 历史"
# ---------------- 下一步 ----------------
next_action:
  - id: retest_yuanbao_request_isolation
    what: "用户连续对两个不同视频点【AI 改写预览】，确认旧视频回包不会覆盖新视频预览，且不会关闭用户系统 Edge"
  - id: retest_yuanbao_debug_login
    what: "用户打开新桌面包，点击【元宝登录】在弹出的调试 Edge 扫码，再点【AI 改写预览】确认不再要求重复登录"
  - id: retest_packaged_upload
    what: "用户关闭旧 exe/旧 python 后打开新 exe，重新上传一个视频验证素材进入 Videos/视频去重素材"
  - id: retest_yuanbao_frame_upload
    what: "用户本机用新构建执行元宝改写，确认日志 frames_b64/frame_files 均大于 0 且图片进入元宝输入框"
  - id: desktop_e2e_regression
    what: "用户本机跑完整回归：探测素材→元宝改写→TTS 去重→下载→打开去重目录→裂变→打开裂变目录"
  - id: retest_tts_warning_scope
    what: "用户本机确认：默认不配音；裂变不读/不透传 TTS；单条去重显式勾选 AI 配音且失败时才弹未生效提醒"
  - id: retest_tts_explicit_gate
    what: "用户本机用新包确认：默认不配音；只有单条去重显式勾选 AI 配音或确认使用改写文案后才启用 TTS；裂变不透传 TTS"
  - id: fission_tts_future
    what: "如后续需要，单独设计 batch_fission 接改写/TTS；当前已明确不支持 TTS 配音"
  - id: archive_legacy_docs
    what: "逐步把旧 web/扩展插件说明迁移到 archive 或历史说明，避免新窗口继续误判当前主线"

# ---------------- 红线 ----------------
constraints:
  desktop_only_current: "当前项目已改为桌面端；web 端、扩展插件是旧版/历史参考，除非用户明确要求，不要再把它们当当前交付主线"
  no_remote_dependency: "桌面端必须跑本机后端 127.0.0.1:8765，禁止依赖远端 124.71.209.36"
  no_web_source_edit: "station/web 是旧参考源，当前 UI 改动集中 archive/web 与 station/desktop/server"
  no_push_without_ask: "用户未明确要求 push 时不得推送；本轮文档更新不提交、不推送"
  no_user_edge_kill: "禁止关闭用户正在使用的系统 Edge；复制/扫码/改写必须走独立调试 profile 或由用户显式处理"
  no_yuanbao_profile_under_package_dist: "元宝 debug profile 禁止放在 package/dist/win-unpacked 等打包产物目录下"
  tts_explicit_gate: "TTS 必须由 enable_tts=true 显式开启；默认不配音，批量裂变不读/不透传 TTS"
  yuanbao_profile_userdata_only: "元宝扫码/改写 profile 只能位于桌面端 userData 持久目录，禁止写入 dist/package 临时或打包产物目录"
  tts_requires_enable_tts: "未收到 enable_tts=true 时，前后端都不得读取、透传或消费 TTS 文案/改写模板"
  output_delete: "禁止自动删除用户产物；目录清理必须先列清单并经用户确认"
  download_path_safety: "下载/打开产物必须保持产物根目录 relative_to 校验，禁止绝对路径或 .. 越界"
  gui_verification: "Agent 沙箱不能验证 Electron GUI/剪映类桌面窗口；端到端验收需用户本机执行"
```
