# Change: <简短标题>

> Change ID: `<add|update|remove|refactor>-<kebab-case-名>`
> 影响级别: <A / B / C>
> 创建日期: YYYY-MM-DD

## Why

<1-2 段。为什么要做。必须引用具体证据：PRD 节点编号 / 用户反馈 / 数据 / 当前链路的具体断点（写清函数名或文件名）。>

<现状描述控制在一句话 + 代码指针，不复述 DD 已有内容。>

## What Changes

### 阶段 A：<阶段名>

- **ADDED** `F-<模块>-<序号>`：<新增什么>
- **MODIFIED** `F-<模块>-<序号>`：<把什么改成什么>

### 阶段 B：<阶段名>

- **REMOVED** `F-<模块>-<序号>`：<废弃什么>

## Impact

- **Affected Features**：`F-xxx-001`、`F-xxx-002`（含被间接影响的调用方）
- **Affected code**：
  - `path/to/file.py`（新增）
  - `path/to/other.py`（修改）
- **新增依赖**：<名称 + 版本 + 体积 / 授权 / 部署要求；无则写「无」>
- **向后兼容性**：<是否破坏性；老数据 / 老客户端如何处理>
- **回归范围**：<需要重新验证的已有功能清单>

### 五项必查清单

逐项回答，无变更也要显式写「无」（空着无法区分「没有」和「忘了查」）：

| # | 检查项 | 本次情况 | 命中后果 |
|---|---|---|---|
| 1 | 接口定义变更 | <有/无 + 说明> | 有 → **强制 design.md** |
| 2 | DB 表字段变更 | <有/无 + 说明> | 有 → **强制 design.md** |
| 3 | 异步消息变更 | <有/无 + 说明> | 消息体结构变更 → **强制 design.md** |
| 4 | 配置项变更 | <有/无 + 说明> | 有 → 同步 `.cursorrules/开发环境配置.md` |
| 5 | 定时任务变更 | <有/无 + 说明> | 有 → 注明是否影响存量任务 |

> 前三项命中即触发 design.md 硬触发线，**与 A/B/C 级别无关**。

## Open Questions

- <未决项>（需 <谁> 在 <何时> 确认）

<最多 3 条。超过 3 条说明资料不足，应改为输出《设计缺失项（Need Clarification）》。>
