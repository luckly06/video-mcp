# Design: <Change ID>

> **硬触发线（命中任一即强制写，A/B/C 无豁免）** —— 判据是可逆性，不是影响半径：
> DB 表字段变更 / 接口定义变更 / 数据结构或 schema 变更（含消息体、配置结构、持久化格式）/ 任何需要「老数据怎么办」的迁移路径。
>
> **软触发（B 级判断参考）**：跨多模块 / 新架构模式 / 新增外部依赖 / 安全性能复杂度高 / 技术选型有多个备选。
>
> C 级且未命中硬触发线 → 可免写本文件。
>
> 本次触发原因：<写明命中了哪一条>

<!-- 命中硬触发线时，Migration Plan 节不得留空 —— 那是本文件存在的主要理由 -->


---

## Context

<背景、约束、相关方。为什么现在要做这个技术决策。>

## Goals / Non-Goals

**Goals**
- <本次要达成什么>

**Non-Goals**
- <明确不做什么，防止范围蔓延>

## Decisions

### 决策 1：<决策标题>

- **选择**：<选了什么>
- **理由**：<为什么>
- **备选方案**：
  | 方案 | 优点 | 缺点 | 为什么不选 |
  |---|---|---|---|
  | <方案 A> | | | |
  | <方案 B> | | | |
- **代价**：<这个选择的代价是什么>

## Risks / Trade-offs

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| <风险描述> | <影响范围> | <怎么防> |

## Migration Plan

**上线顺序**
1. <步骤>

**数据迁移**
- <历史数据怎么处理；是否需要回填 / 清理>

**回滚方案**
- <出问题怎么退；退回去数据还能用吗>

## Open Questions

- <未决项>（需 <谁> 在 <何时> 确认）
