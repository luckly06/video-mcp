# Delta: <Change ID>

> 合并目标：`02-方案设计/02-系统详细设计说明书【DD（Detailed Design）】.md`
> 归档前必须执行合并（见 `05-扩展功能/README.md` 第七节）。

---

## ADDED Features

### F-<模块>-<三位序号>: <Feature 名>

<一句话说明这个 Feature 做什么。>

- **输入**：<依赖的已有 Feature 编号 / 接口 / 数据结构 / 文件；无则写「无」>
- **核心逻辑**：<要实现的业务逻辑，含必要的异常、边界、安全处理>
- **预期产出**：
  - `path/to/file.py`：<新增什么>
  - `alembic/versions/xxx.py`：<迁移脚本，含历史数据处理>

---

## MODIFIED Features

> ⚠️ 必须从 DD 整段复制原文再修改。合并时是**整段替换**，少粘的部分永久消失。
> 如果不是显式改动已有 Feature 的行为，改用 ADDED 新增一条。

### F-<模块>-<三位序号>: <Feature 名（与 DD 完全一致）>

<此处粘贴 DD 里该 Feature 的完整原文，然后在副本上修改。>

- **变更原因**：<为什么要改>
- **输入**：<完整清单，含原有的 + 新增的>
- **核心逻辑**：<完整逻辑，含原有的全部内容 + 本次变更>
- **预期产出**：<完整清单>

---

## REMOVED Features

### F-<模块>-<三位序号>: <Feature 名>

- **Reason**：<为什么废弃>
- **Migration**：<老数据 / 老用户 / 老客户端怎么办>
- **Deprecated Date**：YYYY-MM-DD

<编号保留在 DD 的「已废弃 Features」章节，永不复用。>
