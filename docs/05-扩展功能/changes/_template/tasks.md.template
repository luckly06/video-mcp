# Tasks: <Change ID>

> WBS 增量。归档时同步回 `03-WBS/01-WBS-开发待办.md`。
> `[x]` 只能在**端到端验收通过**后标记，不是「代码写完了」。

---

## 1. <一级节点 —— 必须来自 DD 已有模块划分，不得自创>

### 1.1 <二级节点 —— 按 DD 实现边界或自然依赖拆>

- [ ] 1.1.1 <Work Package 标题>
  - **依赖**：<前置 WP 编号 / `F-` 编号 / 「无」>
  - **验收**：<可验证的具体条件。不写「功能正常」这类无法判定的话>
  - **估时**：Xh

- [ ] 1.1.2 <Work Package 标题>
  - **依赖**：1.1.1
  - **验收**：
  - **估时**：Xh

### 1.2 <二级节点>

- [ ] 1.2.1 <Work Package 标题>
  - **依赖**：
  - **验收**：
  - **估时**：Xh

---

## 2. <另一个一级节点>

### 2.1 <二级节点>

- [ ] 2.1.1 <Work Package 标题>
  - **依赖**：
  - **验收**：
  - **估时**：Xh

---

## 进度

- 总计：0 / 0
- 当前 WP：<正在做哪个>

<每个 Work Package 必须可独立开发、可独立估算、可独立测试验收。>
