# 视频去重工位 station — Python 依赖
# 运行时：仅标准库 + ffmpeg（vendor 自包含），下列为「感知哈希自检」新增的轻量依赖。
# 安装：pip install -r requirements.txt
# 缺失时 metrics.py 自动降级到 ffmpeg signature 兜底（只给 pass/fail，无数值）。

imagehash==4.3.2        # 逐帧 64 位 pHash 感知哈希（PRD D-01 主自检）
Pillow>=10.0            # imagehash 依赖：图像读取
scikit-image>=0.21.0    # SSIM 结构相似度（裂变第三重交付门）
openai>=1.0             # MiMo TTS v2.5 客户端（OpenAI 兼容协议）；缺省时 TTS 功能不可用
sherpa-onnx>=1.10       # 本地 ASR（Paraformer 中文）；缺省时 ASR 不可用
soundfile>=0.12         # sherpa-onnx 依赖：音频文件读取
playwright>=1.40        # DeepSeek 文案改写（浏览器操控）；缺省时改写不可用

# 仅开发/测试用
pytest>=8.0
